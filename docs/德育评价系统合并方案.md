# 德育评价系统与班级管理系统合并方案

## 一、系统现状分析

### 1.1 数据存储对比

| 维度 | 现有班级管理系统 | 德育评价系统（设计） |
|------|-----------------|-------------------|
| 数据库 | SQLite + Excel | MySQL（设计方案） |
| 班级数据 | checkTemplate.xlsx | 数据库表 |
| 学生数据 | checkTemplate.xlsx | 数据库表 |
| 教师数据 | checkTemplate.xlsx | 数据库表 |
| 用户认证 | JWT + Excel | JWT + 数据库 |

### 1.2 现有系统目录结构

```
/Users/yoin/bdsync/program/honghuanghou/lesson/
├── main.py                  # 主入口文件 (FastAPI服务)
├── client.py                # 微信客户端
├── wxmsg.py                 # 微信消息处理
├── sendqueue.py             # 消息队列
├── feishu.py                # 飞书集成
├── websocket.py             # WebSocket服务
├── config/                  # 配置文件目录
│   ├── config.yaml
│   ├── wechat.yaml
│   ├── message.yaml
│   └── lesson.yaml
├── models/                  # 核心模型层
│   ├── manage/
│   │   ├── member.py       # 成员、权限管理
│   │   └── manage.py        # 教学管理(班级/课程)
│   ├── lesson/
│   │   ├── lesson.py        # 核心课程类
│   │   ├── homework.py      # 作业管理
│   │   └── notes.py         # 笔记
│   ├── daily/
│   │   ├── inout.py         # 请假/外出管理
│   │   └── daily.py         # 日常记录
│   └── datas_api/           # API接口层
│       ├── auth.py          # 认证模块
│       ├── admin.py         # 管理员模块
│       ├── teachers.py      # 教师管理
│       └── datas_api_legacy.py # 旧API兼容
├── databases/              # SQLite数据库目录
│   ├── member.db            # 成员/权限数据库
│   ├── homework.db           # 作业数据库
│   ├── inout.db             # 请假数据库
│   └── daily.db             # 日常数据库
├── frontend/               # Vue3前端项目
│   └── src/
│       ├── views/           # 页面组件
│       ├── api/             # API调用模块
│       └── router/         # 路由配置
└── utils/                   # 工具函数
```

### 1.3 现有数据库表结构

#### member.db

**member表 - 会员/成员**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| uuid | TEXT | 唯一标识 |
| wxid | TEXT | 微信ID |
| alias | TEXT | 备注/别名 |
| active | BOOLEAN | 是否激活 |
| score | INTEGER | 积分 |
| balance | INTEGER | 余额 |
| level | INTEGER | 等级(权限级别) |
| model | TEXT | 模块(basic等) |
| ai_flag | BOOLEAN | AI标记 |
| birthday | TEXT | 生日 |
| priority | INTEGER | 优先级 |
| note | TEXT | 备注 |

**permission表 - 权限规则**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| func | TEXT | 功能函数名 |
| func_name | TEXT | 功能中文名 |
| activate | BOOLEAN | 是否启用 |
| black_list | TEXT | 黑名单 |
| white_list | TEXT | 白名单 |
| type | TEXT | 消息类型 |
| pattern | TEXT | 匹配模式(正则) |
| keywords | TEXT | 关键词 |
| level | INTEGER | 权限等级 |
| priority | INTEGER | 优先级 |

#### homework.db

**homework表 - 作业**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| class_code | INTEGER | 班级代码 |
| subject | TEXT | 学科 |
| teacher | TEXT | 教师 |
| content | TEXT | 内容 |
| deadline | TEXT | 截止日期 |
| assigned_at | DATETIME | 发布时间 |
| status | BOOLEAN | 状态 |
| duration | INTEGER | 预计用时(分钟) |
| type | TEXT | 作业类型 |
| wxid | TEXT | 发布人微信ID |
| deleted | INTEGER | 软删除标记 |

**announcements表 - 公告**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| class_code | INTEGER | 班级代码 |
| title | TEXT | 标题 |
| author | TEXT | 作者 |
| content | TEXT | 内容 |
| date | DATETIME | 发布时间 |
| wxid | TEXT | 发布人微信ID |
| deleted | INTEGER | 软删除标记 |

#### inout.db

**inout表 - 请假/外出记录**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| sid | TEXT | 学生ID |
| style | TEXT | 请假类型(事假/病假等) |
| days | TEXT | 天数 |
| status | TEXT | 状态 |
| recorder | TEXT | 记录人 |
| guard | TEXT | 出校门卫 |
| active | BOOLEAN | 是否有效 |
| consumer | TEXT | 销假人 |
| note | TEXT | 备注 |
| create_at | TEXT | 创建时间 |

#### checkTemplate.xlsx

**teachers表 - 教师**
| 字段 | 说明 |
|------|------|
| name | 教师姓名 |
| subject | 科目 |
| course | 课程 |
| pwd | 密码 |
| role | 角色(admin/teacher) |
| level | 等级 |
| active | 登录权限 |
| notice | 通知开关 |
| is_password_changed | 密码是否已修改 |

**students表 - 学生**
| 字段 | 说明 |
|------|------|
| sid | 学生ID |
| name | 姓名 |
| cname | 班级名称 |
| roomid | 微信roomid |
| rpid | 相关ID |
| active | 状态 |

**class表 - 班级模板**
| 字段 | 说明 |
|------|------|
| class_code | 班级代码 |
| class_name | 班级名称 |
| leaders | 班主任/班长 |
| active | 状态 |

### 1.4 现有API接口

```
认证模块 (auth.py):
- POST /token - 用户登录获取JWT令牌

管理员模块 (admin.py):
- GET /admin/users - 获取用户列表
- POST /admin/reset-password - 管理员重置密码
- POST /admin/set-password - 管理员设置密码

教师管理模块 (teachers.py):
- GET /teachers - 获取教师列表
- POST /teachers - 创建教师
- PUT /teachers/{username} - 更新教师信息
- DELETE /teachers/{username} - 删除教师
- POST /teachers/change-password - 修改密码

核心API (datas_api_legacy.py):
- GET /api/class-codes/ - 获取班级代码列表
- GET /api/schedule/{class_name} - 获取班级课表
- GET /api/todays - 获取今日课程
- GET /api/schedules - 获取所有课表
- GET /api/homework/{class_code} - 获取班级作业
- GET /api/announcements/{class_code} - 获取班级公告
- POST /api/homework/ - 发布作业
- POST /api/announcement/ - 发布公告
- GET /api/students/{class_code} - 获取学生列表
- GET /api/students/export/{class_code} - 导出学生
- POST /api/students/import/{class_code} - 导入学生
- GET /api/periods - 获取作息时间
- GET /api/teacher-schedule/{teacher_name} - 教师课表
- GET /api/vehicle-inout/{counts} - 车辆进出
- POST /api/leave-records/ - 提交请假记录
- GET /api/leave-records/ - 获取请假记录
```

### 1.5 现有角色体系

| 角色 | 说明 |
|------|------|
| admin | 管理员(完全权限) |
| teacher | 普通教师 |
| teacher/xuefa | 学发部权限 |

---

## 二、角色体系统一

### 2.1 角色对比与映射

| 现有系统角色 | 德育系统角色 | 对应关系 | 说明 |
|-------------|-------------|---------|------|
| admin | admin | 完全对应 | 管理员 |
| teacher/xuefa | xuefa | 完全对应 | 学发部 |
| teacher | teacher | 完全对应 | 普通教师 |
| - | cleader | 新增 | 班主任（从教师中区分） |
| - | jiaowu | 新增 | 教发部（教师发展部） |
| - | student | 新增 | 学生 |
| - | parent | 新增 | 家长 |

### 2.2 统一角色定义

```python
ROLES = {
    'admin': {
        'name': '管理员',
        'level': 100,
        'permissions': ['all']
    },
    'jiaowu': {
        'name': '教发部',
        'level': 50,
        'permissions': [
            'teacher_manage',            # 教师管理
            'teaching_quality_monitor',  # 教学质量监控
            'academic_diagnosis',        # 学业诊断
            'ai_consultation',           # AI诊疗
            'report_view_all',           # 查看所有报告
            'class_manage',              # 班级管理
            'schedule_manage',           # 课表管理
            'birthday_reminder',         # 生日提醒查看
            'student_profile',           # 学生画像查看
        ]
    },
    'xuefa': {
        'name': '学发部',
        'level': 50,
        'permissions': [
            'moral_record_manage',      # 德育记录管理
            'punishment_manage',         # 处分管理
            'event_type_manage',         # 事件类型配置
            'class_change_approve',      # 班级变更审批
            'report_view_all',           # 查看所有报告
            'student_manage',            # 学生管理
            'ai_consultation',           # AI诊疗
            'birthday_reminder',         # 生日提醒管理
            'student_profile',           # 学生画像管理
        ]
    },
    'cleader': {
        'name': '班主任',
        'level': 30,
        'permissions': [
            'moral_record_own_class',    # 本班德育记录
            'report_view_own_class',     # 本班报告查看
            'homework_publish',          # 发布作业
            'announcement_publish',      # 发布公告
            'leave_approve',             # 请假审批
            'ai_consultation_own_class', # 本班AI诊疗
            'student_profile_own_class', # 本班学生画像
            'birthday_reminder_own_class', # 本班生日提醒
        ]
    },
    'teacher': {
        'name': '教师',
        'level': 10,
        'permissions': [
            'homework_publish',          # 发布作业
            'schedule_view',             # 查看课表
            'student_view',              # 查看学生
            'moral_record_input',        # 录入德育记录
        ]
    },
    'student': {
        'name': '学生',
        'level': 1,
        'permissions': [
            'moral_self_view',           # 查看自己德育
            'homework_view',             # 查看作业
            'schedule_view',             # 查看课表
            'profile_self_view',         # 查看自己画像
            'birthday_blessing_receive', # 接收生日祝福
        ]
    },
    'parent': {
        'name': '家长',
        'level': 1,
        'permissions': [
            'moral_child_view',          # 查看子女德育
            'profile_child_view',        # 查看子女画像
            'birthday_reminder_child',   # 接收子女生日提醒
        ]
    }
}
```

### 2.3 权限检查函数

```python
def check_permission(user, permission: str) -> bool:
    """检查用户是否有某权限"""
    if not user:
        return False

    role = user.role if hasattr(user, 'role') else user.get('role', '')
    role_config = ROLES.get(role, {})

    # admin拥有所有权限
    if 'all' in role_config.get('permissions', []):
        return True

    return permission in role_config.get('permissions', [])


def check_class_access(user, class_id: int) -> bool:
    """检查用户是否有班级访问权限"""
    if check_permission(user, 'report_view_all'):
        return True

    if check_permission(user, 'report_view_own_class'):
        # 班主任只能访问自己的班级
        teacher_class = get_teacher_class(user.teacher_id)
        return teacher_class == class_id

    return False
```

---

## 三、数据合并方案

### 3.1 合并原则

```
┌─────────────────────────────────────────────────────────────┐
│                    合并原则                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 最小改动原则                                            │
│     ├── 保留现有系统核心功能                                │
│     ├── 新增德育模块，不破坏现有逻辑                        │
│     └── 兼容现有数据格式                                    │
│                                                             │
│  2. 数据统一原则                                            │
│     ├── 班级、学生、教师数据统一管理                        │
│     ├── 一处维护，多处使用                                  │
│     └── 保持数据一致性                                      │
│                                                             │
│  3. 渐进式迁移原则                                          │
│     ├── Excel→数据库 分步迁移                               │
│     ├── 保留过渡期兼容                                      │
│     └── 无缝切换                                            │
│                                                             │
│  4. 权限统一原则                                            │
│     ├── 统一角色定义                                        │
│     ├── 统一认证机制                                        │
│     └── 细粒度权限控制                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 方案选择

#### 方案A：统一到数据库（推荐）

```
优势：
- 数据结构清晰，便于维护
- 支持复杂查询和统计
- 事务支持，数据一致性好
- 便于德育评价功能实现

劣势：
- 需要迁移现有Excel数据
- 需要调整部分现有代码
```

#### 方案B：保持Excel + 新增德育数据库

```
优势：
- 现有系统改动最小
- 快速上线

劣势：
- 数据分散，维护困难
- 数据一致性难保证
- 德育统计查询效率低

不推荐原因：
- 德育评价涉及大量统计计算
- Excel不适合频繁写入和复杂查询
```

**结论：推荐方案A，统一到数据库**

### 3.3 统一数据模型

```sql
-- ============================================
-- 基础表（合并现有系统 + 德育系统）
-- ============================================

-- 1. 级号表（新增）
CREATE TABLE grade (
    grade_id INT PRIMARY KEY AUTO_INCREMENT,
    grade_name VARCHAR(10) NOT NULL COMMENT '如：2025级',
    enrollment_year INT NOT NULL COMMENT '入学年份',
    UNIQUE KEY uk_enrollment_year (enrollment_year)
);

-- 1.1 年级等级配置表（用于判断高一/高二/高三）
CREATE TABLE grade_level_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    years_after_enrollment INT NOT NULL COMMENT '入学后第几年',
    level_name VARCHAR(10) NOT NULL COMMENT '高一/高二/高三',
    UNIQUE KEY uk_years (years_after_enrollment)
);

INSERT INTO grade_level_config VALUES
(1, 0, '高一'),
(2, 1, '高二'),
(3, 2, '高三');

-- 2. 班级表（合并现有class + 德育class）
CREATE TABLE class (
    class_id INT PRIMARY KEY AUTO_INCREMENT,
    class_code VARCHAR(10) NOT NULL COMMENT '班级代码，如：202501',
    grade_id INT NOT NULL COMMENT '所属级号',
    class_number INT NOT NULL COMMENT '班号',
    class_name VARCHAR(20) NOT NULL COMMENT '班级名称，如：2025级1班',
    leader_wxid VARCHAR(50) COMMENT '班主任微信ID',
    leader_name VARCHAR(20) COMMENT '班主任姓名',
    roomid VARCHAR(50) COMMENT '微信班级群ID',
    is_active TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT NOW(),
    UNIQUE KEY uk_class_code (class_code),
    UNIQUE KEY uk_grade_class (grade_id, class_number),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id)
);

-- 3. 学生表（合并现有students + 德育student + 扩展信息）
CREATE TABLE student (
    student_id VARCHAR(20) PRIMARY KEY COMMENT '学号',
    name VARCHAR(20) NOT NULL,
    gender ENUM('男', '女'),
    class_id INT NOT NULL COMMENT '当前班级ID',
    grade_id INT NOT NULL COMMENT '当前级号ID',
    original_grade_id INT COMMENT '入学时级号',
    roomid VARCHAR(50) COMMENT '微信ID',
    status ENUM('在校', '休学', '转出', '毕业') DEFAULT '在校',
    status_date DATE,
    enrollment_date DATE COMMENT '入学日期',
    is_active TINYINT DEFAULT 1,

    -- 扩展信息：个人信息
    birthday DATE COMMENT '出生日期',
    phone VARCHAR(20) COMMENT '联系电话',
    email VARCHAR(50) COMMENT '电子邮箱',

    -- 扩展信息：学业信息
    entrance_score DECIMAL(6,2) COMMENT '入学成绩（中考成绩）',
    entrance_rank INT COMMENT '入学排名',
    middle_school VARCHAR(100) COMMENT '初中学校名称',
    middle_school_city VARCHAR(50) COMMENT '初中学校所在城市',

    -- 扩展信息：毕业去向
    gaokao_score DECIMAL(6,2) COMMENT '高考成绩',
    gaokao_year INT COMMENT '高考年份',
    gaokao_rank INT COMMENT '高考排名',
    university_name VARCHAR(100) COMMENT '大学院校名称',
    university_type VARCHAR(20) COMMENT '院校类型：985/211/一本/二本/专科',
    university_major VARCHAR(50) COMMENT '录取专业',
    university_city VARCHAR(50) COMMENT '大学所在城市',

    -- 扩展信息：AI画像
    profile_summary TEXT COMMENT '学生画像摘要',
    profile_tags JSON COMMENT '画像标签',
    profile_updated_at DATETIME COMMENT '画像更新时间',

    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW(),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id),
    FOREIGN KEY (original_grade_id) REFERENCES grade(grade_id),
    INDEX idx_birthday (birthday),
    INDEX idx_grade_status (grade_id, status)
);

-- 4. 教师表（合并现有teachers + 德育角色）
CREATE TABLE teacher (
    teacher_id VARCHAR(20) PRIMARY KEY COMMENT '工号',
    name VARCHAR(20) NOT NULL,
    wxid VARCHAR(50) COMMENT '微信ID',
    subject VARCHAR(20) COMMENT '任教学科',
    password_hash VARCHAR(255) COMMENT '密码哈希',
    role VARCHAR(50) DEFAULT 'teacher' COMMENT '角色：admin/jiaowu/xuefa/cleader/teacher',
    level INT DEFAULT 0 COMMENT '权限等级',
    is_active TINYINT DEFAULT 1 COMMENT '登录权限',
    notice_enabled TINYINT DEFAULT 1 COMMENT '通知开关',
    is_password_changed TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW()
);

-- 5. 班级履历表（德育新增）
CREATE TABLE student_class_history (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    class_id INT NOT NULL,
    grade_id INT NOT NULL,
    start_date DATE NOT NULL,
    end_date DATE,
    change_reason VARCHAR(50),
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id),
    INDEX idx_student_date (student_id, start_date, end_date)
);

-- 5.1 学籍变动记录表
CREATE TABLE student_status_change (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    change_type VARCHAR(20) NOT NULL COMMENT '休学/复学/转入/转出/毕业',
    from_class_id INT,
    to_class_id INT,
    from_grade_id INT,
    to_grade_id INT,
    effective_date DATE NOT NULL,
    reason TEXT,
    approver VARCHAR(20),
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (from_class_id) REFERENCES class(class_id),
    FOREIGN KEY (to_class_id) REFERENCES class(class_id),
    FOREIGN KEY (from_grade_id) REFERENCES grade(grade_id),
    FOREIGN KEY (to_grade_id) REFERENCES grade(grade_id)
);

-- 6. 学年表（先创建，被semester表引用）
CREATE TABLE school_year (
    year_id INT PRIMARY KEY AUTO_INCREMENT,
    year_name VARCHAR(20) NOT NULL COMMENT '如：2025-2026学年',
    start_date DATE NOT NULL,
    end_date DATE NOT NULL,
    is_current TINYINT DEFAULT 0,
    UNIQUE KEY uk_year_name (year_name)
);

-- 7. 学期表（德育新增）
CREATE TABLE semester (
    semester_id INT PRIMARY KEY AUTO_INCREMENT,
    semester_name VARCHAR(20) NOT NULL,
    year_id INT NOT NULL COMMENT '学年ID',
    start_date DATE NOT NULL,
    end_date DATE NOT NULL,
    status TINYINT DEFAULT 1 COMMENT '1=当前学期',
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (year_id) REFERENCES school_year(year_id)
);

-- ============================================
-- 德育评价核心表
-- ============================================

-- 8. 日常事件类型
CREATE TABLE daily_event_type (
    event_id INT PRIMARY KEY AUTO_INCREMENT,
    event_name VARCHAR(50) NOT NULL,
    event_type TINYINT NOT NULL COMMENT '1=积极 2=消极',
    score INT NOT NULL,
    description TEXT,
    is_active TINYINT DEFAULT 1,
    CONSTRAINT chk_score_type CHECK (
        (event_type = 1 AND score > 0) OR
        (event_type = 2 AND score < 0)
    )
);

-- 9. 校级事件类型
CREATE TABLE school_event_type (
    event_id INT PRIMARY KEY AUTO_INCREMENT,
    event_name VARCHAR(100) NOT NULL,
    event_level VARCHAR(20) COMMENT '校级/市级/省级/国家级',
    event_type TINYINT NOT NULL COMMENT '1=荣誉 2=处分',
    score INT NOT NULL,
    description TEXT,
    is_active TINYINT DEFAULT 1,
    CONSTRAINT chk_school_score_type CHECK (
        (event_type = 1 AND score > 0) OR
        (event_type = 2 AND score < 0)
    )
);

-- 10. 年级德育任务
CREATE TABLE grade_moral_task (
    task_id INT PRIMARY KEY AUTO_INCREMENT,
    grade_id INT NOT NULL,
    task_name VARCHAR(100) NOT NULL,
    task_desc TEXT,
    score INT NOT NULL,
    year_id INT NOT NULL,
    can_carryover TINYINT DEFAULT 1,
    max_carryover_times INT DEFAULT 2,
    is_active TINYINT DEFAULT 1,
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id),
    FOREIGN KEY (year_id) REFERENCES school_year(year_id)
);

-- 11. 日常表现记录
CREATE TABLE student_daily_record (
    record_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    event_id INT NOT NULL,
    semester_id INT NOT NULL,
    record_date DATE NOT NULL,
    class_id INT NOT NULL,
    grade_id INT NOT NULL,
    score INT,
    remark TEXT,
    recorder VARCHAR(20),
    is_deleted TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (event_id) REFERENCES daily_event_type(event_id),
    FOREIGN KEY (semester_id) REFERENCES semester(semester_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id),
    INDEX idx_student_semester (student_id, semester_id),
    UNIQUE KEY uk_student_event_date (student_id, event_id, record_date, semester_id)
);

-- 12. 校级事件记录
CREATE TABLE student_school_record (
    record_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    event_id INT NOT NULL,
    semester_id INT NOT NULL,
    get_date DATE NOT NULL,
    class_id INT NOT NULL,
    grade_id INT NOT NULL,
    score INT,
    proof VARCHAR(255),
    is_deleted TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (event_id) REFERENCES school_event_type(event_id),
    FOREIGN KEY (semester_id) REFERENCES semester(semester_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id)
);

-- 13. 学生任务完成记录
CREATE TABLE student_task_finish (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    task_id INT NOT NULL,
    year_id INT NOT NULL,
    original_task_id INT,
    original_year_id INT,
    is_carried_over TINYINT DEFAULT 0,
    carryover_count INT DEFAULT 0,
    current_score DECIMAL(6,2),
    status TINYINT DEFAULT 0 COMMENT '0=未完成 1=已完成 2=已作废',
    finish_date DATE,
    finish_year_id INT,
    proof TEXT,
    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (task_id) REFERENCES grade_moral_task(task_id),
    FOREIGN KEY (year_id) REFERENCES school_year(year_id),
    UNIQUE KEY uk_student_task_year (student_id, task_id, year_id)
);

-- 14. 处分记录
CREATE TABLE punishment_record (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    event_id INT NOT NULL,
    semester_id INT NOT NULL,
    punishment_date DATE NOT NULL,
    class_id INT NOT NULL,
    grade_id INT NOT NULL,
    score_deduct INT NOT NULL,
    level VARCHAR(20),
    reason TEXT,
    recorder VARCHAR(20),
    is_revoked TINYINT DEFAULT 0,
    revoke_date DATE,
    revoke_by VARCHAR(20),
    revoke_reason TEXT,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (event_id) REFERENCES school_event_type(event_id),
    FOREIGN KEY (semester_id) REFERENCES semester(semester_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id)
);

-- 15. 集体事件表
CREATE TABLE collective_event (
    event_id INT PRIMARY KEY AUTO_INCREMENT,
    event_name VARCHAR(100) NOT NULL,
    event_type VARCHAR(20) NOT NULL,
    semester_id INT NOT NULL,
    event_date DATE NOT NULL,
    score INT NOT NULL,
    description TEXT,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (semester_id) REFERENCES semester(semester_id)
);

-- 16. 集体事件分配
CREATE TABLE collective_event_distribution (
    id INT PRIMARY KEY AUTO_INCREMENT,
    event_id INT NOT NULL,
    student_id VARCHAR(20) NOT NULL,
    class_id INT NOT NULL,
    score_assigned INT,
    is_participant TINYINT DEFAULT 1,
    remark TEXT,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (event_id) REFERENCES collective_event(event_id),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    UNIQUE KEY uk_event_student (event_id, student_id)
);

-- 17. 德育评价
CREATE TABLE moral_evaluation (
    eval_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    semester_id INT NOT NULL,
    class_id INT NOT NULL,
    grade_id INT NOT NULL,
    total_score DECIMAL(6,2) DEFAULT 0,
    level VARCHAR(10),
    update_time DATETIME DEFAULT NOW(),
    UNIQUE KEY uk_student_semester (student_id, semester_id),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (semester_id) REFERENCES semester(semester_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id)
);

-- 18. 任务结转日志
CREATE TABLE task_carryover_log (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    original_task_id INT NOT NULL,
    from_year_id INT NOT NULL,
    to_year_id INT NOT NULL,
    carryover_index INT NOT NULL,
    score_before DECIMAL(6,2),
    score_after DECIMAL(6,2),
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (original_task_id) REFERENCES grade_moral_task(task_id),
    FOREIGN KEY (from_year_id) REFERENCES school_year(year_id),
    FOREIGN KEY (to_year_id) REFERENCES school_year(year_id)
);

-- 19. 违纪累进规则
CREATE TABLE violation_escalation_rule (
    rule_id INT PRIMARY KEY AUTO_INCREMENT,
    event_id INT NOT NULL,
    time_window_days INT DEFAULT 90,
    escalation_rules JSON,
    FOREIGN KEY (event_id) REFERENCES daily_event_type(event_id)
);

-- 20. 操作日志
CREATE TABLE moral_operation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    operator VARCHAR(20) NOT NULL,
    operator_role VARCHAR(20),
    operation VARCHAR(20) NOT NULL,
    table_name VARCHAR(50) NOT NULL,
    record_id INT,
    semester_id INT,
    old_data JSON,
    new_data JSON,
    reason TEXT,
    ip_address VARCHAR(50),
    created_at DATETIME DEFAULT NOW(),
    INDEX idx_operator (operator),
    INDEX idx_table_record (table_name, record_id)
);

-- 21. 学期结转配置表
CREATE TABLE semester_carryover_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    data_type VARCHAR(20) NOT NULL COMMENT 'honor/punishment/task',
    can_carryover TINYINT DEFAULT 1,
    score_factor DECIMAL(3,2) DEFAULT 1.00 COMMENT '结转分值系数',
    description TEXT
);

INSERT INTO semester_carryover_config (data_type, can_carryover, score_factor, description) VALUES
('honor', 0, 1.00, '荣誉不结转，记录保留在原学期'),
('punishment', 0, 1.00, '处分不结转，记录保留在原学期'),
('task_unfinished', 1, 0.60, '未完成任务可结转，每次×60%');

-- 22. 数据可见性配置表
CREATE TABLE data_visibility_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    data_type VARCHAR(50) NOT NULL,
    visible_roles JSON NOT NULL COMMENT '["admin", "xuefa"]',
    description TEXT
);

INSERT INTO data_visibility_config (data_type, visible_roles, description) VALUES
('punishment_record', '["admin", "xuefa", "jiaowu"]', '处分记录'),
('daily_negative', '["admin", "xuefa", "jiaowu", "cleader"]', '消极事件'),
('daily_positive', '["admin", "xuefa", "jiaowu", "cleader", "teacher"]', '积极事件'),
('honor_record', '["admin", "xuefa", "jiaowu", "cleader", "teacher", "student", "parent"]', '荣誉记录'),
('evaluation_score', '["admin", "xuefa", "jiaowu", "cleader", "student", "parent"]', '评价得分');

-- 23. 预警配置表
CREATE TABLE warning_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    rule_name VARCHAR(50) NOT NULL,
    trigger_type VARCHAR(20) NOT NULL COMMENT 'score_threshold/count_threshold',
    trigger_value INT NOT NULL,
    notify_roles JSON COMMENT '["cleader", "xuefa"]',
    is_active TINYINT DEFAULT 1
);

INSERT INTO warning_config (rule_name, trigger_type, trigger_value, notify_roles) VALUES
('德育分过低', 'score_threshold', 50, '["cleader", "xuefa", "jiaowu"]'),
('扣分过多', 'score_threshold', -20, '["cleader", "xuefa", "jiaowu"]'),
('违纪次数过多', 'count_threshold', 5, '["cleader", "xuefa", "jiaowu"]');

-- 24. 预警日志表
CREATE TABLE warning_log (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    rule_id INT NOT NULL,
    semester_id INT NOT NULL,
    warning_level VARCHAR(20) DEFAULT 'warning',
    message TEXT,
    is_read TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (rule_id) REFERENCES warning_config(id),
    FOREIGN KEY (semester_id) REFERENCES semester(semester_id)
);

-- ============================================
-- 扩展功能：生日提醒、学生画像、AI诊疗
-- ============================================

-- 25. 生日提醒配置表
CREATE TABLE birthday_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    config_type VARCHAR(20) NOT NULL COMMENT 'reminder/blessing/template',
    is_enabled TINYINT DEFAULT 1,
    notify_roles JSON COMMENT '通知角色列表',
    blessing_template TEXT COMMENT '祝福语模板',
    reminder_time VARCHAR(10) DEFAULT '08:00',
    advance_days INT DEFAULT 0,
    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW()
);

INSERT INTO birthday_config (config_type, is_enabled, notify_roles, blessing_template) VALUES
('reminder', 1, '["cleader", "xuefa", "jiaowu"]', '今日是 {student_name} 的生日，请关注'),
('blessing', 1, '["student"]', '🎂 生日快乐！\n亲爱的 {student_name}，今天是你的生日，祝你生日快乐，学业进步，前程似锦！\n\n——来自德育评价系统');

-- 26. 生日祝福记录表
CREATE TABLE birthday_log (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    birthday_date DATE NOT NULL,
    blessing_type VARCHAR(20) COMMENT 'wechat/system/email',
    blessing_content TEXT,
    sent_at DATETIME,
    sent_to VARCHAR(50),
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    INDEX idx_birthday_date (birthday_date)
);

-- 27. 学生画像表
CREATE TABLE student_profile (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    profile_version INT DEFAULT 1,
    profile_summary TEXT COMMENT '画像摘要',
    profile_tags JSON COMMENT '画像标签数组',
    strength_tags JSON COMMENT '优势标签',
    improvement_tags JSON COMMENT '待改进标签',
    risk_level VARCHAR(10) COMMENT '风险等级：low/medium/high',
    moral_score DECIMAL(5,2) COMMENT '品德评分',
    attitude_score DECIMAL(5,2) COMMENT '态度评分',
    social_score DECIMAL(5,2) COMMENT '社交评分',
    growth_score DECIMAL(5,2) COMMENT '成长评分',
    suggestions TEXT COMMENT '个性化建议',
    intervention_priority VARCHAR(10) COMMENT '干预优先级',
    data_source_summary JSON COMMENT '数据来源统计',
    generated_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    UNIQUE KEY uk_student_version (student_id, profile_version)
);

-- 28. 画像历史表
CREATE TABLE student_profile_history (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    profile_version INT NOT NULL,
    profile_data JSON COMMENT '完整画像数据快照',
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id)
);

-- 28.1 画像配置表
CREATE TABLE profile_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    config_key VARCHAR(50) NOT NULL,
    config_value JSON,
    description TEXT,
    UNIQUE KEY uk_key (config_key)
);

INSERT INTO profile_config VALUES
(1, 'tag_definitions', '["责任担当", "诚实守信", "乐于助人", "勤奋刻苦", "积极进取", "团结协作", "遵纪守法", "文明礼貌", "关爱他人", "勇于创新"]', '画像标签定义'),
(2, 'update_frequency', '{"type": "semester", "min_records": 5}', '更新频率配置'),
(3, 'risk_thresholds', '{"high": {"negative_count": 10, "score_below": 50}, "medium": {"negative_count": 5, "score_below": 60}}', '风险阈值配置');

-- 29. AI诊疗会话表
CREATE TABLE ai_consultation (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    consultation_type VARCHAR(20) NOT NULL COMMENT 'academic/behavior/psychological/comprehensive',
    title VARCHAR(100),
    description TEXT,
    status VARCHAR(20) DEFAULT 'active' COMMENT 'active/resolved/closed',
    priority VARCHAR(10) DEFAULT 'normal' COMMENT 'urgent/high/normal/low',
    creator VARCHAR(20),
    assignee VARCHAR(20),
    participants JSON,
    ai_analysis TEXT COMMENT 'AI分析报告',
    ai_suggestions JSON COMMENT 'AI建议列表',
    ai_risk_assessment TEXT COMMENT '风险评估',
    solution TEXT,
    outcome VARCHAR(20),
    follow_up_date DATE,
    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW(),
    closed_at DATETIME,
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    INDEX idx_student_status (student_id, status)
);

-- 30. AI诊疗对话记录表
CREATE TABLE ai_consultation_message (
    id INT PRIMARY KEY AUTO_INCREMENT,
    consultation_id INT NOT NULL,
    sender_type VARCHAR(20) NOT NULL COMMENT 'user/ai',
    sender_id VARCHAR(20),
    message_type VARCHAR(20) DEFAULT 'text' COMMENT 'text/analysis/suggestion/report',
    content TEXT NOT NULL,
    metadata JSON,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (consultation_id) REFERENCES ai_consultation(id)
);

-- 31. AI诊疗模板表
CREATE TABLE ai_consultation_template (
    id INT PRIMARY KEY AUTO_INCREMENT,
    template_type VARCHAR(20) NOT NULL COMMENT 'academic/behavior/psychological',
    template_name VARCHAR(50) NOT NULL,
    prompt_template TEXT NOT NULL,
    suggested_questions JSON,
    response_format JSON,
    is_active TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT NOW()
);

INSERT INTO ai_consultation_template VALUES
(1, 'academic', '学业问题诊断', '学生{student_name}近期出现学业问题：{description}。请分析可能的原因并提供解决方案。', '["学习成绩下降", "作业完成困难", "课堂注意力不集中"]', '{}', 1, NOW()),
(2, 'behavior', '行为问题诊断', '学生{student_name}出现行为问题：{description}。请分析原因并提供干预建议。', '["迟到早退", "违纪行为", "人际关系冲突"]', '{}', 1, NOW()),
(3, 'psychological', '心理问题诊断', '学生{student_name}可能出现心理困扰：{description}。请提供初步评估和建议。', '["情绪波动", "社交退缩", "压力过大"]', '{}', 1, NOW());

-- ============================================
-- 现有系统表迁移（保留兼容）
-- ============================================

-- 32. 作业表（保留，增加关联）
CREATE TABLE homework (
    id INT PRIMARY KEY AUTO_INCREMENT,
    class_code VARCHAR(10) NOT NULL,
    class_id INT COMMENT '关联班级ID',
    subject VARCHAR(20),
    teacher VARCHAR(20),
    teacher_id VARCHAR(20) COMMENT '关联教师ID',
    content TEXT,
    deadline DATE,
    assigned_at DATETIME,
    status TINYINT DEFAULT 1,
    duration INT,
    type VARCHAR(20),
    wxid VARCHAR(50),
    deleted TINYINT DEFAULT 0,
    INDEX idx_class_code (class_code),
    INDEX idx_class_id (class_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    FOREIGN KEY (teacher_id) REFERENCES teacher(teacher_id)
);

-- 33. 公告表（保留，增加关联）
CREATE TABLE announcement (
    id INT PRIMARY KEY AUTO_INCREMENT,
    class_code VARCHAR(10) NOT NULL,
    class_id INT,
    title VARCHAR(100),
    author VARCHAR(20),
    author_id VARCHAR(20),
    content TEXT,
    date DATETIME,
    wxid VARCHAR(50),
    deleted TINYINT DEFAULT 0,
    INDEX idx_class_code (class_code),
    INDEX idx_class_id (class_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id)
);

-- 34. 请假表（保留，增加关联）
CREATE TABLE leave_record (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    class_id INT,
    style VARCHAR(20),
    days DECIMAL(3,1),
    start_date DATE,
    end_date DATE,
    status VARCHAR(20) DEFAULT '待审批',
    recorder VARCHAR(20),
    approver VARCHAR(20),
    approve_time DATETIME,
    consumer VARCHAR(20),
    consume_time DATETIME,
    note TEXT,
    is_active TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id)
);
```

---

## 四、API合并方案

### 4.1 保留不变的API

```python
# 现有API（完全保留）

GET  /api/class-codes/           # 获取班级代码列表
GET  /api/schedule/{class_name}  # 获取班级课表
GET  /api/todays                 # 获取今日课程
GET  /api/homework/{class_code}  # 获取班级作业
GET  /api/announcements/{class_code}  # 获取班级公告
POST /api/homework/              # 发布作业
POST /api/announcement/          # 发布公告
GET  /api/periods                # 获取作息时间
GET  /api/teacher-schedule/{teacher_name}  # 教师课表
POST /api/token                  # 登录认证
```

### 4.2 需要修改的API

```python
# 学生管理API（改为数据库）

# 原：读取Excel checkTemplate.xlsx
# 改：读取数据库student表

GET  /api/students/{class_code}
     # 修改：从数据库读取
     # SELECT s.*, c.class_name FROM student s JOIN class c ON s.class_id = c.class_id

POST /api/students/import/{class_code}
     # 修改：写入数据库，同时创建student_class_history

GET  /api/students/export/{class_code}
     # 修改：从数据库导出Excel

# 请假API（扩展）
POST /api/leave-records/
     # 修改：使用student_id关联，写入leave_record表
GET  /api/leave-records/
     # 修改：从leave_record表读取
```

### 4.3 新增德育API

```python
# 德育评价API（新增）

# 日常表现
POST   /api/moral/daily-records           # 录入日常表现
GET    /api/moral/daily-records           # 查询记录列表
GET    /api/moral/daily-records/{student_id}  # 学生记录
POST   /api/moral/daily-records/import    # 批量导入
GET    /api/moral/daily-records/template  # 下载模板

# 校级事件
POST   /api/moral/school-records          # 录入校级事件
GET    /api/moral/school-records          # 查询事件列表
PUT    /api/moral/school-records/{id}     # 修改事件

# 处分管理
POST   /api/moral/punishments             # 录入处分
GET    /api/moral/punishments             # 查询处分列表
PUT    /api/moral/punishments/{id}/revoke # 撤销处分

# 德育任务
GET    /api/moral/tasks                   # 任务列表
POST   /api/moral/tasks                   # 创建任务
PUT    /api/moral/tasks/{task_id}         # 修改任务
DELETE /api/moral/tasks/{task_id}         # 删除任务
POST   /api/moral/task-finish             # 完成任务
POST   /api/moral/tasks/carryover         # 任务结转

# 集体事件
POST   /api/moral/collective-events       # 创建集体事件
GET    /api/moral/collective-events       # 集体事件列表
POST   /api/moral/collective-events/{id}/distribute  # 分配

# 评价查询
GET    /api/moral/evaluation/student/{student_id}  # 学生评价
GET    /api/moral/evaluation/class/{class_id}      # 班级统计
GET    /api/moral/evaluation/grade/{grade_id}      # 年级统计

# 趋势分析
GET    /api/moral/analysis/student/{student_id}/trend  # 学生趋势
GET    /api/moral/analysis/class/{class_id}/compare    # 班级对比

# 报告导出
GET    /api/moral/reports/student/{student_id}        # 学生报告单
GET    /api/moral/reports/class/{class_id}            # 班级报表
GET    /api/moral/reports/export/student/{student_id} # 导出PDF
GET    /api/moral/reports/export/class/{class_id}     # 导出Excel

# 事件类型配置
GET    /api/moral/event-types             # 事件类型列表
POST   /api/moral/event-types/daily       # 添加日常事件类型
POST   /api/moral/event-types/school      # 添加校级事件类型
PUT    /api/moral/event-types/daily/{id}  # 编辑日常事件类型
PUT    /api/moral/event-types/school/{id} # 编辑校级事件类型
DELETE /api/moral/event-types/daily/{id}  # 删除/禁用事件类型

# 班级变更
POST   /api/moral/students/{student_id}/class-change  # 班级变更
GET    /api/moral/students/{student_id}/history       # 班级履历

# 系统初始化
GET    /api/moral/import/status           # 导入状态
GET    /api/moral/import/check-dependency/{type}  # 检查依赖
POST   /api/moral/import/classes          # 导入班级
POST   /api/moral/import/students         # 导入学生
GET    /api/moral/validate                # 数据验证
```

### 4.4 API权限配置

```yaml
# 德育评价API权限配置

# 日常表现
/api/moral/daily-records:
  allowed_roles: [cleader, teacher, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/daily-records/import:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

# 校级事件
/api/moral/school-records:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

# 处分管理
/api/moral/punishments:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

/api/moral/punishments/{id}/revoke:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 10

# 任务管理
/api/moral/tasks:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

/api/moral/tasks/carryover:
  allowed_roles: [xuefa, admin]
  min_level: 10

# 集体事件
/api/moral/collective-events:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

# 评价查询
/api/moral/evaluation/student/{student_id}:
  allowed_roles: [cleader, xuefa, jiaowu, admin, student, parent]
  min_level: 0

/api/moral/evaluation/class/{class_id}:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/evaluation/grade/{grade_id}:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

# 事件类型配置
/api/moral/event-types/daily:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

/api/moral/event-types/daily/{id}:
  allowed_roles: [admin]
  min_level: 100

# 班级变更
/api/moral/students/{student_id}/class-change:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 10

# 系统初始化
/api/moral/import/status:
  allowed_roles: [admin, xuefa, jiaowu]
  min_level: 5

/api/moral/import/classes:
  allowed_roles: [admin, xuefa, jiaowu]
  min_level: 5

/api/moral/import/students:
  allowed_roles: [admin, xuefa, jiaowu]
  min_level: 5

/api/moral/validate:
  allowed_roles: [admin, xuefa, jiaowu]
  min_level: 5

# 生日提醒
/api/moral/birthday/today:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/birthday/upcoming:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/birthday/send-blessing/{student_id}:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

/api/moral/birthday/config:
  allowed_roles: [admin]
  min_level: 100

# 学生画像
/api/moral/profile/{student_id}:
  allowed_roles: [cleader, xuefa, jiaowu, admin, student, parent]
  min_level: 0

/api/moral/profile/generate/{student_id}:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

/api/moral/profile/batch-generate:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

/api/moral/profile/history/{student_id}:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

# AI诊疗
/api/moral/consultation:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/consultation/{id}:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/consultation/create:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/consultation/{id}/ai-analyze:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

/api/moral/consultation/templates:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

/api/moral/consultation/{id}/message:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/consultation/{id}/solution:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/consultation/{id}/close:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/api/moral/consultation/student/{student_id}:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

# 学生扩展信息
/api/moral/students/{student_id}/extended-info:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

/api/moral/students/graduates:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5
```

---

## 五、数据迁移方案

### 5.1 迁移流程

```
┌─────────────────────────────────────────────────────────────┐
│                    数据迁移流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  阶段一：数据库准备                                          │
│  ├── 创建统一数据库表结构                                   │
│  ├── 创建索引和约束                                         │
│  └── 配置数据库连接                                         │
│                                                             │
│  阶段二：基础数据迁移                                        │
│  ├── 导入学年、学期配置                                     │
│  ├── 从Excel提取级号信息                                    │
│  ├── 迁移班级数据 → class表                                 │
│  └── 创建级号-班级关联                                      │
│                                                             │
│  阶段三：核心数据迁移                                        │
│  ├── 迁移学生数据 → student表                               │
│  ├── 创建学生班级履历                                       │
│  ├── 迁移教师数据 → teacher表                               │
│  └── 迁移作业、公告等数据                                   │
│                                                             │
│  阶段四：验证与切换                                          │
│  ├── 数据完整性验证                                         │
│  ├── API功能测试                                            │
│  ├── 灰度发布                                               │
│  └── 正式切换                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 迁移脚本

```python
# scripts/migrate_data.py
#
# 注意：以下代码使用 MySQL 占位符 %s
# 如使用 SQLite，请将 %s 替换为 ?

import pandas as pd
from datetime import date

class DataMigrator:
    """数据迁移工具"""

    def __init__(self, excel_path: str, db):
        self.excel_path = excel_path
        self.db = db

    def migrate_all(self):
        """执行完整迁移"""
        print("=" * 60)
        print("开始数据迁移...")
        print("=" * 60)

        # 1. 迁移级号
        self.migrate_grades()

        # 2. 迁移班级
        self.migrate_classes()

        # 3. 迁移学生
        self.migrate_students()

        # 4. 迁移教师
        self.migrate_teachers()

        print("\n" + "=" * 60)
        print("数据迁移完成！")
        print("=" * 60)

    def migrate_grades(self):
        """从班级代码提取级号"""
        print("\n[1] 迁移级号数据...")

        df = pd.read_excel(self.excel_path, sheet_name='class')

        # 从class_code提取级号（如202501 → 2025级）
        class_codes = df['class_code'].astype(str).tolist()
        years = set()
        for code in class_codes:
            if len(code) >= 4:
                years.add(int(code[:4]))

        count = 0
        for year in sorted(years):
            self.db.execute("""
                INSERT INTO grade (grade_name, enrollment_year)
                VALUES (%s, %s)
                ON DUPLICATE KEY UPDATE grade_name = VALUES(grade_name)
            """, f"{year}级", year)
            count += 1

        print(f"  迁移了 {count} 个级号")

    def migrate_classes(self):
        """迁移班级数据"""
        print("\n[2] 迁移班级数据...")

        df = pd.read_excel(self.excel_path, sheet_name='class')

        count = 0
        for _, row in df.iterrows():
            class_code = str(row['class_code'])
            class_name = str(row['class_name'])

            # 解析级号和班号
            if len(class_code) >= 6:
                year = int(class_code[:4])
                class_number = int(class_code[4:])
            else:
                continue

            # 获取级号ID
            grade = self.db.query(
                "SELECT grade_id FROM grade WHERE enrollment_year = %s", year
            ).first()

            if not grade:
                continue

            # 插入班级
            self.db.execute("""
                INSERT INTO class (class_code, grade_id, class_number, class_name,
                                   leader_name, is_active)
                VALUES (%s, %s, %s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE class_name = VALUES(class_name)
            """, class_code, grade['grade_id'], class_number, class_name,
                 row.get('leaders'), row.get('active', 1))

            count += 1

        print(f"  迁移了 {count} 个班级")

    def migrate_students(self):
        """迁移学生数据"""
        print("\n[3] 迁移学生数据...")

        df = pd.read_excel(self.excel_path, sheet_name='students')

        count = 0
        errors = []

        for _, row in df.iterrows():
            sid = str(row['sid'])
            name = str(row['name'])
            cname = str(row.get('cname', ''))
            roomid = str(row.get('roomid', ''))

            # 根据班级名称查找班级
            cls = self.db.query(
                "SELECT class_id, grade_id FROM class WHERE class_name = %s", cname
            ).first()

            if not cls:
                # 尝试从sid解析班级
                if len(sid) >= 6:
                    class_code = sid[:6]
                    cls = self.db.query(
                        "SELECT class_id, grade_id FROM class WHERE class_code = %s", class_code
                    ).first()

            if not cls:
                errors.append(f"学生 {sid}({name}) 找不到班级")
                continue

            # 插入学生
            self.db.execute("""
                INSERT INTO student (student_id, name, class_id, grade_id, roomid,
                                     original_grade_id, status, is_active)
                VALUES (%s, %s, %s, %s, %s, %s, '在校', 1)
                ON DUPLICATE KEY UPDATE name = VALUES(name), class_id = VALUES(class_id)
            """, sid, name, cls['class_id'], cls['grade_id'], roomid, cls['grade_id'])

            # 创建班级履历
            self.db.execute("""
                INSERT INTO student_class_history
                (student_id, class_id, grade_id, start_date, change_reason)
                VALUES (%s, %s, %s, %s, '入学')
                ON DUPLICATE KEY UPDATE end_date = NULL
            """, sid, cls['class_id'], cls['grade_id'], '2025-09-01')

            count += 1

        print(f"  迁移了 {count} 个学生")
        if errors:
            print(f"  ⚠️ {len(errors)} 个学生迁移失败")
            for e in errors[:5]:
                print(f"     - {e}")

    def migrate_teachers(self):
        """迁移教师数据"""
        print("\n[4] 迁移教师数据...")

        df = pd.read_excel(self.excel_path, sheet_name='teachers')

        count = 0
        for _, row in df.iterrows():
            name = str(row['name'])
            subject = str(row.get('subject', ''))
            role = str(row.get('role', 'teacher'))
            level = int(row.get('level', 0))
            active = int(row.get('active', 1))
            pwd_changed = int(row.get('is_password_changed', 0))
            pwd = str(row.get('pwd', ''))

            # 生成教师ID
            teacher_id = f"T{name}"

            # 密码哈希
            if pwd and pwd_changed:
                password_hash = hash_password(pwd)
            else:
                password_hash = hash_password('123456')

            self.db.execute("""
                INSERT INTO teacher (teacher_id, name, subject, password_hash,
                                     role, level, is_active, is_password_changed)
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE name = VALUES(name), role = VALUES(role)
            """, teacher_id, name, subject, password_hash, role, level, active, pwd_changed)

            count += 1

        print(f"  迁移了 {count} 个教师")


def hash_password(password: str) -> str:
    """密码哈希"""
    from passlib.context import CryptContext
    pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
    return pwd_context.hash(password)


if __name__ == "__main__":
    # 执行迁移
    migrator = DataMigrator('checkTemplate.xlsx', db)
    migrator.migrate_all()
```

### 5.3 数据验证

```python
def validate_migration(db):
    """验证迁移结果"""

    print("=" * 60)
    print("数据迁移验证")
    print("=" * 60)

    errors = []

    # 1. 数据量验证
    print("\n[1] 数据量验证")

    checks = [
        ("级号", "grade", "SELECT COUNT(*) as cnt FROM grade"),
        ("班级", "class", "SELECT COUNT(*) as cnt FROM class"),
        ("学生", "student", "SELECT COUNT(*) as cnt FROM student"),
        ("教师", "teacher", "SELECT COUNT(*) as cnt FROM teacher"),
    ]

    for name, table, query in checks:
        result = db.query(query).first()
        count = result['cnt']
        status = "✅" if count > 0 else "❌"
        print(f"  {status} {name}: {count} 条")
        if count == 0:
            errors.append(f"{name}数据为空")

    # 2. 关联验证
    print("\n[2] 关联验证")

    # 学生-班级关联
    orphan = db.query("""
        SELECT COUNT(*) as cnt FROM student s
        LEFT JOIN class c ON s.class_id = c.class_id
        WHERE c.class_id IS NULL
    """).first()['cnt']
    print(f"  {'✅' if orphan == 0 else '❌'} 学生-班级关联: {orphan} 条异常")

    # 班级履历验证
    no_history = db.query("""
        SELECT COUNT(*) as cnt FROM student s
        LEFT JOIN student_class_history sch ON s.student_id = sch.student_id
        WHERE s.status = '在校' AND sch.id IS NULL
    """).first()['cnt']
    print(f"  {'✅' if no_history == 0 else '❌'} 班级履历: {no_history} 人缺失")

    # 3. 结果
    print("\n" + "=" * 60)
    if not errors:
        print("✅ 验证通过")
    else:
        print("❌ 发现问题:")
        for e in errors:
            print(f"   - {e}")

    return errors
```

---

## 六、前端合并方案

### 6.1 菜单结构

```
┌─────────────────────────────────────────────────────────────┐
│                    系统菜单结构                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  首页                                                       │
│  ├── 今日课程                                              │
│  └── 消息通知                                              │
│                                                             │
│  教学管理                                                   │
│  ├── 课程表                                                │
│  ├── 作业管理                                              │
│  ├── 公告管理                                              │
│  └── 早读安排                                              │
│                                                             │
│  学生管理                                                   │
│  ├── 学生列表                                              │
│  ├── 请假管理                                              │
│  └── 学生导入/导出                                         │
│                                                             │
│  德育评价 [新增]                                            │
│  ├── 日常表现录入                                          │
│  ├── 校级事件录入                                          │
│  ├── 德育任务管理                                          │
│  ├── 处分管理                                              │
│  ├── 评价查询                                              │
│  ├── 统计分析                                              │
│  └── 报告导出                                              │
│                                                             │
│  系统设置                                                   │
│  ├── 教师管理                                              │
│  ├── 事件类型配置 [新增]                                    │
│  ├── 班级管理                                              │
│  └── 权限配置                                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 路由配置

```javascript
// router/index.js

const routes = [
  // 现有路由（保留）
  { path: '/', component: Home },
  { path: '/schedule', component: Schedule },
  { path: '/homework', component: Homework },
  { path: '/announcement', component: Announcement },
  { path: '/students', component: ClassStudents },
  { path: '/leave', component: LeaveRecord },

  // 德育评价路由（新增）
  {
    path: '/moral',
    component: MoralLayout,
    meta: { requiresAuth: true },
    children: [
      {
        path: 'daily',
        name: 'MoralDaily',
        component: () => import('@/views/moral/DailyRecord.vue'),
        meta: {
          title: '日常表现录入',
          permission: 'moral_record_own_class'
        }
      },
      {
        path: 'school-events',
        name: 'MoralSchoolEvents',
        component: () => import('@/views/moral/SchoolEvents.vue'),
        meta: {
          title: '校级事件录入',
          permission: 'moral_record_manage'
        }
      },
      {
        path: 'tasks',
        name: 'MoralTasks',
        component: () => import('@/views/moral/TaskManage.vue'),
        meta: {
          title: '德育任务管理',
          permission: 'moral_record_own_class'
        }
      },
      {
        path: 'punishments',
        name: 'MoralPunishments',
        component: () => import('@/views/moral/PunishmentManage.vue'),
        meta: {
          title: '处分管理',
          permission: 'punishment_manage'
        }
      },
      {
        path: 'evaluation',
        name: 'MoralEvaluation',
        component: () => import('@/views/moral/Evaluation.vue'),
        meta: {
          title: '评价查询',
          permission: 'moral_self_view'
        }
      },
      {
        path: 'reports',
        name: 'MoralReports',
        component: () => import('@/views/moral/Reports.vue'),
        meta: {
          title: '报告导出',
          permission: 'report_view_own_class'
        }
      },
      {
        path: 'event-types',
        name: 'MoralEventTypes',
        component: () => import('@/views/moral/EventTypes.vue'),
        meta: {
          title: '事件类型配置',
          permission: 'event_type_manage'
        }
      },
    ]
  },
]

// 权限检查
router.beforeEach((to, from, next) => {
  if (to.meta.permission) {
    const user = store.state.user
    if (!checkPermission(user, to.meta.permission)) {
      next('/403')
      return
    }
  }
  next()
})
```

### 6.3 API模块

```javascript
// api/moral.js

import request from '@/utils/request'

// 日常表现
export function getDailyRecords(params) {
  return request.get('/api/moral/daily-records', { params })
}

export function createDailyRecord(data) {
  return request.post('/api/moral/daily-records', data)
}

export function importDailyRecords(formData) {
  return request.post('/api/moral/daily-records/import', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}

// 校级事件
export function getSchoolRecords(params) {
  return request.get('/api/moral/school-records', { params })
}

export function createSchoolRecord(data) {
  return request.post('/api/moral/school-records', data)
}

// 处分管理
export function getPunishments(params) {
  return request.get('/api/moral/punishments', { params })
}

export function createPunishment(data) {
  return request.post('/api/moral/punishments', data)
}

export function revokePunishment(id, data) {
  return request.put(`/api/moral/punishments/${id}/revoke`, data)
}

// 德育任务
export function getTasks(params) {
  return request.get('/api/moral/tasks', { params })
}

export function createTask(data) {
  return request.post('/api/moral/tasks', data)
}

export function finishTask(data) {
  return request.post('/api/moral/task-finish', data)
}

// 评价查询
export function getStudentEvaluation(studentId, semesterId) {
  return request.get(`/api/moral/evaluation/student/${studentId}`, {
    params: { semester_id: semesterId }
  })
}

export function getClassEvaluation(classId, semesterId) {
  return request.get(`/api/moral/evaluation/class/${classId}`, {
    params: { semester_id: semesterId }
  })
}

// 报告导出
export function exportStudentReport(studentId) {
  return request.get(`/api/moral/reports/export/student/${studentId}`, {
    responseType: 'blob'
  })
}

export function exportClassReport(classId) {
  return request.get(`/api/moral/reports/export/class/${classId}`, {
    responseType: 'blob'
  })
}

// 事件类型
export function getEventTypes(params) {
  return request.get('/api/moral/event-types', { params })
}

export function createDailyEventType(data) {
  return request.post('/api/moral/event-types/daily', data)
}

export function updateDailyEventType(id, data) {
  return request.put(`/api/moral/event-types/daily/${id}`, data)
}
```

---

## 七、验证方案

### 7.1 数据迁移验证

| 验证项 | 检查内容 | 方法 |
|-------|---------|------|
| 数据量一致性 | Excel记录数 = 数据库记录数 | COUNT对比 |
| 数据完整性 | 必填字段不为空 | NULL检查 |
| 关联完整性 | 外键关联正确 | JOIN检查 |
| 数据准确性 | 关键字段值一致 | 抽样对比 |

### 7.2 功能验证

| 模块 | 验证项 | 预期结果 |
|------|-------|---------|
| 认证 | 登录功能 | 使用迁移后的教师账号可登录 |
| 课表 | 课表查询 | 显示正确课表 |
| 作业 | 作业发布/查看 | 功能正常 |
| 学生 | 学生列表 | 显示正确学生数据 |
| 德育 | 日常表现录入 | 可正常录入并计算 |
| 德育 | 评价查询 | 显示正确评价结果 |
| 德育 | 报告导出 | 可正常导出PDF/Excel |

### 7.3 兼容性验证

| 验证项 | 说明 |
|-------|------|
| 微信机器人 | 现有微信消息功能正常 |
| 前端页面 | 所有页面正常访问 |
| API接口 | 现有API返回正确数据 |
| 权限控制 | 权限限制正确生效 |

---

## 八、实施计划

```
┌─────────────────────────────────────────────────────────────┐
│                    实施计划                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  第1周：准备阶段                                            │
│  ├── 设计详细数据库表结构                                   │
│  ├── 编写迁移脚本                                          │
│  └── 搭建测试环境                                          │
│                                                             │
│  第2周：数据迁移                                            │
│  ├── 执行数据迁移                                          │
│  ├── 数据验证                                              │
│  └── 问题修复                                              │
│                                                             │
│  第3周：API开发                                             │
│  ├── 修改现有API（改用数据库）                              │
│  ├── 开发德育评价新API                                      │
│  └── API测试                                               │
│                                                             │
│  第4周：前端开发                                            │
│  ├── 开发德育评价页面                                       │
│  ├── 集成到现有系统                                        │
│  └── 界面测试                                              │
│                                                             │
│  第5周：集成测试                                            │
│  ├── 功能测试                                              │
│  ├── 性能测试                                              │
│  └── 用户验收测试                                          │
│                                                             │
│  第6周：上线                                                │
│  ├── 灰度发布                                              │
│  ├── 正式上线                                              │
│  └── 运维监控                                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 九、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| 数据迁移丢失 | 高 | 多次备份、迁移前后对比验证 |
| API兼容性问题 | 中 | 保留原有API签名、充分测试 |
| 前端页面冲突 | 中 | 模块化开发、路由隔离 |
| 权限配置错误 | 中 | 权限矩阵设计、测试验证 |
| 微信机器人故障 | 高 | 保留原有逻辑、充分测试 |

---

## 十、总结

### 合并要点

1. **数据统一**：Excel → 数据库，一处维护多处使用
2. **权限统一**：扩展角色体系，细粒度权限控制
3. **API兼容**：保留现有API，新增德育API
4. **渐进迁移**：分步迁移，充分验证

### 预期效果

- 现有功能保持不变
- 新增德育评价功能
- 数据管理更规范
- 权限控制更精细
- 系统可扩展性增强

---

*文档版本：v1.6*
*最后更新：2026-03-28*