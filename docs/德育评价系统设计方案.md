# 高中学生德育评价管理系统 - 完整设计方案

## 一、系统概述

### 1.1 评价体系架构

```
┌─────────────────────────────────────────────────────────────┐
│                        德育评价系统                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐   │
│  │  数据输入    │ → │  数据分析    │ → │  评价赋分    │   │
│  └──────────────┘   └──────────────┘   └──────────────┘   │
│         ↓                  ↓                  ↓            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    数据展示                          │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 四大模块定位

| 模块 | 功能 | 说明 |
|------|------|------|
| **数据输入** | 录入数据 | 日常表现、荣誉处分、任务完成 |
| **数据分析** | 统计分析 | 班级排名、事件统计、趋势分析 |
| **评价赋分** | 评分定级 | 计算总分、判定等级 |
| **数据展示** | 查询展示 | 个人报告、班级报表、导出打印 |

### 1.3 四大数据输入来源

| 输入类型 | 内容 | 分值影响 |
|---------|------|---------|
| 年级德育任务 | 按年级设定的必修德育项目 | 完成后加分 |
| 日常常规表现 | 积极事件 / 消极事件 | 加分 / 扣分 |
| 校级及以上事件 | 荣誉 / 处分 | 加分 / 扣分 |
| 集体事件 | 班级集体荣誉/活动 | 每人加分 |

### 1.4 计分规则

```
德育总分 = 基础分(60) + 日常表现分 + 校级事件分 + 任务完成奖励分
```

| 分值项 | 范围 |
|-------|------|
| 基础分 | 60分 |
| 积极事件 | +1 ~ +5 分 |
| 消极事件 | -1 ~ -5 分 |
| 校级荣誉 | +5 ~ +20 分 |
| 校级处分 | -5 ~ -30 分 |
| 任务完成 | +10 ~ +30 分/项 |

**注意：德育总分可超过100分**

### 1.5 等级评定

| 等级 | 分数范围 |
|------|---------|
| 优秀 | ≥ 90 分（无上限） |
| 良好 | 80 ~ 89 分 |
| 合格 | 60 ~ 79 分 |
| 不合格 | < 60 分 |

---

## 二、核心业务规则

### 2.1 学期数据流转

| 规则 | 说明 |
|------|------|
| 学期计算 | 每学期重新计算，历史数据保留 |
| 多学期统计 | 支持跨学期数据汇总查询 |
| 荣誉结转 | 不结转（记录保留，分值留在当学期） |
| 处分结转 | 不结转（记录保留，分值留在当学期） |
| 任务结转 | 未完成任务可结转，详见任务规则 |

### 2.2 任务规则（以学年为单位）

#### 任务周期

| 项目 | 说明 |
|------|------|
| 任务周期 | 以学年为单位（一个学年 = 两个学期） |
| 完成期限 | 一个学年内完成 |
| 结转时机 | 学年末处理 |

#### 任务结转规则

| 规则 | 说明 |
|------|------|
| 已完成任务 | 分值保留在完成学年，不结转 |
| 未完成任务 | 可结转到下一学年 |
| 结转分值 | 每次结转 ×60%（递减） |
| 最大结转次数 | 2次（高一→高二→高三） |
| 超过限制 | 任务作废，不得分 |

#### 任务结转示例

```
任务：技能考核A
原始分值：30分
原始学年：2025-2026学年（高一）

┌────────────────────────────────────────────────────────────────────┐
│ 学年          │ 状态   │ 结转次数 │ 当前可得分数 │ 操作           │
├────────────────────────────────────────────────────────────────────┤
│ 2025-2026     │ 未完成 │ 0        │ 30.00       │ 学年末结转     │
│ 2026-2027     │ 未完成 │ 1        │ 18.00       │ 学年末结转     │
│ 2027-2028     │ 完成   │ 2        │ 10.80       │ 得10.80分      │
└────────────────────────────────────────────────────────────────────┘

计算公式：30 × (0.6)² = 10.80分
```

### 2.3 集体事件处理

#### 集体事件类型

| 类型 | 说明 | 分值处理 |
|------|------|---------|
| 班级荣誉 | 班级获得集体荣誉（如先进班集体） | 每人加固定分 |
| 集体活动 | 班级参与集体活动（如运动会、合唱比赛） | 每人加固定分 |
| 集体违纪 | 班级集体违纪 | 每人扣固定分 |

#### 集体事件分配规则

```
班级集体荣誉示例：

事件：2025级1班获得"先进班集体"称号
分值：每人+5分
分配方式：全班所有在校学生

处理流程：
1. 创建集体事件记录
2. 自动为该班所有在校学生分配分数
3. 学生可标记是否实际参与（默认参与）
4. 未参与者可申请不加分
```

### 2.4 学生状态与班级调整

#### 年级与班级概念

```
级号（grade）: 入学年份，如 2025级、2024级
班级（class）: 如 2025级1班、2025级2班
年级等级: 高一/高二/高三（动态计算）

规则：
- 级号属于班级，班级永远属于某个级号
- 学生有级号归属（留级时会变）
- 自然升年级：级号不变，年级等级+1（高一→高二）
```

#### 级号与年级等级对应关系

| 级号 | 2025-2026学年 | 2026-2027学年 | 2027-2028学年 |
|------|--------------|--------------|--------------|
| 2025级 | 高一 | 高二 | 高三 |
| 2024级 | 高二 | 高三 | 毕业 |
| 2023级 | 高三 | 毕业 | - |

#### 学生状态

| 状态 | 说明 | 德育记录 | 参与统计 | 任务处理 |
|------|------|---------|---------|---------|
| 在校 | 正常就读 | 正常记录 | 参与 | 正常进行 |
| 休学 | 暂停学业 | 暂停记录 | 不参与 | 任务冻结 |
| 复学 | 恢复就读 | 恢复记录 | 参与 | 任务解冻继续 |
| 转入 | 外校转入 | 开始记录 | 参与 | 新分配任务 |
| 转出 | 离开本校 | 停止记录 | 不参与 | 任务作废 |
| 毕业 | 完成学业 | 结束记录 | 可查询历史 | 未完成任务作废 |

#### 休学期间任务冻结规则

| 规则 | 说明 |
|------|------|
| 休学时 | 未完成任务状态保持，不参与结转，计时暂停 |
| 复学时 | 任务解冻，继续计算，结转次数不变 |
| 冻结期间 | 任务不计入任何学年统计 |
| 复学后结转 | 按正常结转规则处理 |

#### 班级变更类型

| 变更类型 | 级号变化 | 班级变化 | 说明 |
|---------|---------|---------|------|
| 入学 | 设置 | 设置 | 新生入学 |
| 调班 | 不变 | 变 | 同级调班 |
| 留级 | 变大 | 变 | 去更晚入学的级（如2024级→2025级） |
| 复学 | 可能变 | 变 | 休学后复学，通常降级 |
| 转入 | 设置 | 设置 | 外校转入 |
| 转出 | 不变 | 离开 | 离开本校 |
| 休学 | 不变 | 不变 | 暂停学业 |
| 毕业 | 不变 | 不变 | 完成学业 |

#### 留级示例

```
学生A：2024级（高二）留级

当前状态：
- grade_id = 2024级（高二）

留级后：
- grade_id = 2025级（高一）
- class_id = 2025级某班

级号变化：2024 → 2025（变大）
原因：留级是去重新读低年级，低年级对应更晚入学的级号
```

### 2.5 处分与违纪处理

| 规则 | 说明 |
|------|------|
| 处分可撤销 | 撤销后扣分不返还 |
| 累进加重 | 同类违纪在指定时间内多次，额外扣分 |
| 警告不自动解除 | 到期后需手动处理 |
| 无申诉渠道 | 不提供申诉功能 |

#### 累进加重机制

```
示例：迟到

90天内违纪次数与扣分：
- 第1次：按原分值扣分
- 第2次：额外扣1分
- 第3次：额外扣3分
- 第4次及以上：额外扣5分
```

### 2.6 数据可见性

| 数据类型 | 可见角色 |
|---------|---------|
| 处分记录 | 管理员、学发部 |
| 消极事件 | 管理员、学发部、班主任 |
| 积极事件 | 管理员、学发部、班主任、教师 |
| 荣誉记录 | 全员可见 |
| 评价得分 | 管理员、学发部、班主任、学生、家长 |

**班主任额外限制：只能查看本班学生数据**

### 2.7 预警机制

| 规则 | 说明 |
|------|------|
| 扣分不通知家长 | 仅内部预警 |
| 预警对象 | 班主任、学发部 |

| 预警类型 | 触发条件 | 通知对象 |
|---------|---------|---------|
| 德育分过低 | 总分 < 50分 | 班主任、学发部 |
| 扣分过多 | 累计扣分 > 20分 | 班主任、学发部 |
| 违纪次数过多 | 学期内违纪 ≥ 5次 | 班主任、学发部 |

### 2.8 数据展示

#### 展示维度

| 展示维度 | 内容 | 适用角色 |
|---------|------|---------|
| **个人维度** | 学生德育报告单、历史记录、趋势图 | 学生、家长、班主任 |
| **班级维度** | 班级排名、平均分、优秀率 | 班主任、学发部 |
| **年级维度** | 年级对比、班级排名 | 学发部、管理员 |
| **全校维度** | 整体统计、预警汇总 | 管理员 |

#### 展示内容

**1. 学生德育报告单**

```
┌──────────────────────────────────────────────────────────────┐
│                    XX中学学生德育评价报告单                   │
├──────────────────────────────────────────────────────────────┤
│ 学号：________  姓名：________  班级：________  学期：______ │
├──────────────────────────────────────────────────────────────┤
│ 一、基础信息                                                 │
│     基础分：60分                                             │
├──────────────────────────────────────────────────────────────┤
│ 二、日常表现                                                 │
│     积极事件：____ 次，加分 ____ 分                          │
│     消极事件：____ 次，扣分 ____ 分                          │
│     日常表现得分：____ 分                                    │
├──────────────────────────────────────────────────────────────┤
│ 三、校级事件                                                 │
│     荣誉：____ 次，加分 ____ 分                              │
│     处分：____ 次，扣分 ____ 分                              │
│     校级事件得分：____ 分                                    │
├──────────────────────────────────────────────────────────────┤
│ 四、德育任务                                                 │
│     已完成任务：____ 个，得分 ____ 分                        │
│     未完成任务：____ 个                                      │
├──────────────────────────────────────────────────────────────┤
│ 五、学期总评                                                 │
│     总分：____ 分                                            │
│     等级：□优秀 □良好 □合格 □不合格                         │
├──────────────────────────────────────────────────────────────┤
│ 班主任签字：________    德育处盖章：________    日期：______ │
└──────────────────────────────────────────────────────────────┘
```

**2. 班级德育统计表**

| 统计项 | 数值 |
|-------|------|
| 学生人数 | ____ 人 |
| 平均分 | ____ 分 |
| 最高分 | ____ 分 |
| 最低分 | ____ 分 |
| 优秀人数 | ____ 人（___%） |
| 良好人数 | ____ 人（___%） |
| 合格人数 | ____ 人（___%） |
| 不合格人数 | ____ 人（___%） |
| 积极事件总数 | ____ 次 |
| 消极事件总数 | ____ 次 |
| 荣誉次数 | ____ 次 |
| 处分次数 | ____ 次 |

**3. 班级排名表**

| 排名 | 学号 | 姓名 | 总分 | 等级 | 班级排名 | 年级排名 |
|-----|------|------|------|------|---------|---------|
| 1 | A001 | 张三 | 125 | 优秀 | 1 | 3 |
| 2 | A002 | 李四 | 118 | 优秀 | 2 | 8 |

#### 导出功能

| 导出类型 | 格式 | 说明 |
|---------|------|------|
| 个人报告单 | PDF | 可打印，含盖章位置 |
| 班级统计表 | Excel | 便于存档 |
| 学生明细 | Excel | 含所有记录明细 |
| 年级汇总 | Excel | 各班级对比数据 |

---

## 三、数据库设计

### 3.1 表结构总览

```
基础配置表:
├── semester                    # 学期
├── school_year                 # 学年
├── grade                       # 级号
├── grade_level_config          # 年级等级配置（高一/高二/高三）
├── class                       # 班级
├── student                     # 学生
└── role                        # 角色

事件配置表:
├── daily_event_type            # 日常事件类型
├── school_event_type           # 校级事件类型
├── grade_moral_task            # 年级德育任务
├── violation_escalation_rule   # 违纪累进规则

系统配置表:
├── semester_carryover_config   # 学期结转配置
├── data_visibility_config      # 数据可见性配置
├── warning_config              # 预警配置

记录表:
├── student_class_history       # 学生班级履历
├── student_status_change       # 学籍变动记录
├── student_daily_record        # 日常表现记录
├── student_school_record       # 校级事件记录
├── student_task_finish         # 任务完成记录
├── punishment_record           # 处分记录
├── collective_event            # 集体事件
├── collective_event_distribution # 集体事件分配
├── task_carryover_log          # 任务结转日志
├── warning_log                 # 预警日志

评价表:
├── moral_evaluation            # 德育评价
├── moral_operation_log         # 操作日志
```

### 3.2 数据完整性约束

```
┌─────────────────────────────────────────────────────────────┐
│                     数据完整性保障                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  实体完整性（主键）                                          │
│  ├── 所有表均有主键                                         │
│  └── 使用 AUTO_INCREMENT 或业务主键                         │
│                                                             │
│  参照完整性（外键）                                          │
│  ├── 记录表关联学生表                                       │
│  ├── 记录表关联事件类型表                                   │
│  └── 班级履历关联班级/级号表                                │
│                                                             │
│  域完整性（约束）                                            │
│  ├── 积极事件分数必须 > 0                                   │
│  ├── 消极事件分数必须 < 0                                   │
│  ├── 学生状态枚举限制                                       │
│  └── 任务状态枚举限制                                       │
│                                                             │
│  业务完整性（唯一约束）                                      │
│  ├── 同一学生同一天同一事件只能录入一次                      │
│  ├── 同一荣誉证书编号唯一                                   │
│  └── 同一处分文件编号唯一                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 基础配置表

```sql
-- 学期表
CREATE TABLE semester (
    semester_id INT PRIMARY KEY AUTO_INCREMENT,
    semester_name VARCHAR(20) NOT NULL COMMENT '如：2025-2026上',
    year_id INT NOT NULL COMMENT '所属学年',
    start_date DATE NOT NULL,
    end_date DATE NOT NULL,
    status TINYINT DEFAULT 1 COMMENT '1=当前学期',
    FOREIGN KEY (year_id) REFERENCES school_year(year_id)
);

-- 学年表
CREATE TABLE school_year (
    year_id INT PRIMARY KEY AUTO_INCREMENT,
    year_name VARCHAR(20) NOT NULL COMMENT '如：2025-2026学年',
    start_date DATE NOT NULL,
    end_date DATE NOT NULL,
    is_current TINYINT DEFAULT 0 COMMENT '是否当前学年',
    UNIQUE KEY uk_year_name (year_name)
);

-- 级号表
CREATE TABLE grade (
    grade_id INT PRIMARY KEY AUTO_INCREMENT,
    grade_name VARCHAR(10) NOT NULL COMMENT '如：2025级',
    enrollment_year INT NOT NULL COMMENT '入学年份',
    UNIQUE KEY uk_enrollment_year (enrollment_year)
);

-- 年级等级配置表
CREATE TABLE grade_level_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    years_after_enrollment INT NOT NULL COMMENT '入学后第几年',
    level_name VARCHAR(10) NOT NULL COMMENT '高一/高二/高三',
    UNIQUE KEY uk_years (years_after_enrollment)
);

INSERT INTO grade_level_config VALUES
(1, 0, '高一'),
(2, 1, '高二'),
(3, 2, '高三');

-- 班级表
CREATE TABLE class (
    class_id INT PRIMARY KEY AUTO_INCREMENT,
    grade_id INT NOT NULL COMMENT '所属级号',
    class_number INT NOT NULL COMMENT '班号',
    UNIQUE KEY uk_grade_class (grade_id, class_number),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id)
);

-- 学生表（扩展版）
CREATE TABLE student (
    student_id VARCHAR(20) PRIMARY KEY COMMENT '学号',
    name VARCHAR(20) NOT NULL,
    gender ENUM('男', '女'),
    grade_id INT NOT NULL COMMENT '当前级号（留级时会变）',
    original_grade_id INT COMMENT '入学时级号（不变）',
    status ENUM('在校', '休学', '转出', '毕业') DEFAULT '在校' COMMENT '学生状态',
    status_date DATE COMMENT '状态变更日期',
    enrollment_date DATE COMMENT '入学日期',

    -- 新增：个人信息扩展
    birthday DATE COMMENT '出生日期',
    phone VARCHAR(20) COMMENT '联系电话',
    email VARCHAR(50) COMMENT '电子邮箱',

    -- 新增：学业信息扩展
    entrance_score DECIMAL(6,2) COMMENT '入学成绩（中考成绩）',
    entrance_rank INT COMMENT '入学排名',

    -- 新增：毕业去向信息（毕业后填写）
    gaokao_score DECIMAL(6,2) COMMENT '高考成绩',
    gaokao_year INT COMMENT '高考年份',
    gaokao_rank INT COMMENT '高考排名',

    -- 新增：教育经历
    middle_school VARCHAR(100) COMMENT '初中学校名称',
    middle_school_city VARCHAR(50) COMMENT '初中学校所在城市',

    -- 新增：升学去向
    university_name VARCHAR(100) COMMENT '大学院校名称',
    university_type VARCHAR(20) COMMENT '院校类型：985/211/一本/二本/专科',
    university_major VARCHAR(50) COMMENT '录取专业',
    university_city VARCHAR(50) COMMENT '大学所在城市',

    -- 新增：AI画像相关
    profile_summary TEXT COMMENT '学生画像摘要（AI生成）',
    profile_tags JSON COMMENT '画像标签数组',
    profile_updated_at DATETIME COMMENT '画像更新时间',

    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW(),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id),
    FOREIGN KEY (original_grade_id) REFERENCES grade(grade_id),
    INDEX idx_birthday (birthday) COMMENT '生日索引（用于生日提醒）',
    INDEX idx_grade_status (grade_id, status) COMMENT '年级状态索引'
);

-- 角色表
CREATE TABLE role (
    role_id VARCHAR(20) PRIMARY KEY,
    role_name VARCHAR(50),
    description TEXT
);

INSERT INTO role VALUES
('admin', '管理员', '系统管理员'),
('jiaowu', '教发部', '教师发展部，负责教学质量、教师管理'),
('xuefa', '学发部', '学生发展部，负责德育评价'),
('cleader', '班主任', '班级管理者'),
('teacher', '教师', '普通教师'),
('student', '学生', '学生'),
('parent', '家长', '学生家长');
```

### 3.4 学生班级履历表

```sql
-- 学生班级履历（核心：记录学生在各时期的班级和级号归属）
CREATE TABLE student_class_history (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    class_id INT NOT NULL COMMENT '班级ID',
    grade_id INT NOT NULL COMMENT '当时的级号',
    start_date DATE NOT NULL COMMENT '生效开始日期',
    end_date DATE COMMENT '生效结束日期，NULL=当前',
    change_reason VARCHAR(50) COMMENT '入学/调班/留级/复学/转入',
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id),
    INDEX idx_student_date (student_id, start_date, end_date)
);

-- 学籍变动记录
CREATE TABLE student_status_change (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    change_type VARCHAR(20) NOT NULL COMMENT '休学/复学/转入/转出/毕业',
    from_class_id INT,
    to_class_id INT,
    from_grade_id INT,
    to_grade_id INT,
    effective_date DATE NOT NULL,
    reason TEXT,
    approver VARCHAR(20),
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id)
);
```

### 3.5 事件配置表

```sql
-- 日常事件类型（含分数范围校验）
CREATE TABLE daily_event_type (
    event_id INT PRIMARY KEY AUTO_INCREMENT,
    event_name VARCHAR(50) NOT NULL,
    event_type TINYINT NOT NULL COMMENT '1=积极 2=消极',
    score INT NOT NULL COMMENT '加分/扣分',
    description TEXT,
    is_active TINYINT DEFAULT 1,
    -- 分数校验：积极事件分数>0，消极事件分数<0
    CONSTRAINT chk_score_type CHECK (
        (event_type = 1 AND score > 0) OR
        (event_type = 2 AND score < 0)
    )
);

-- 校级事件类型（含分数范围校验）
CREATE TABLE school_event_type (
    event_id INT PRIMARY KEY AUTO_INCREMENT,
    event_name VARCHAR(100) NOT NULL,
    event_level VARCHAR(20) COMMENT '校级/市级/省级/国家级',
    event_type TINYINT NOT NULL COMMENT '1=荣誉 2=处分',
    score INT NOT NULL,
    description TEXT,
    is_active TINYINT DEFAULT 1,
    -- 分数校验
    CONSTRAINT chk_school_score_type CHECK (
        (event_type = 1 AND score > 0) OR
        (event_type = 2 AND score < 0)
    )
);

-- 年级德育任务（以学年为单位）
CREATE TABLE grade_moral_task (
    task_id INT PRIMARY KEY AUTO_INCREMENT,
    grade_id INT NOT NULL COMMENT '适用级号',
    task_name VARCHAR(100) NOT NULL,
    task_desc TEXT,
    score INT NOT NULL COMMENT '完成后获得分数',
    year_id INT NOT NULL COMMENT '所属学年',
    can_carryover TINYINT DEFAULT 1 COMMENT '是否允许结转',
    max_carryover_times INT DEFAULT 2 COMMENT '最大结转次数',
    is_active TINYINT DEFAULT 1,
    FOREIGN KEY (grade_id) REFERENCES grade(grade_id),
    FOREIGN KEY (year_id) REFERENCES school_year(year_id)
);

-- 违纪累进规则
CREATE TABLE violation_escalation_rule (
    rule_id INT PRIMARY KEY AUTO_INCREMENT,
    event_id INT NOT NULL COMMENT '违纪事件类型',
    time_window_days INT DEFAULT 90 COMMENT '统计时间窗口',
    escalation_rules JSON COMMENT '累进规则',
    -- 示例: [{"count":2,"extra_deduct":1},{"count":3,"extra_deduct":3}]
    FOREIGN KEY (event_id) REFERENCES daily_event_type(event_id)
);
```

### 3.6 记录表

```sql
-- 日常表现记录（含唯一约束防重复）
CREATE TABLE student_daily_record (
    record_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    event_id INT NOT NULL,
    semester_id INT NOT NULL,
    record_date DATE NOT NULL,
    class_id INT NOT NULL COMMENT '记录时所属班级（快照）',
    grade_id INT NOT NULL COMMENT '记录时所属级号（快照）',
    score INT COMMENT '得分/扣分（冗余）',
    remark TEXT,
    recorder VARCHAR(20),
    is_deleted TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (event_id) REFERENCES daily_event_type(event_id),
    INDEX idx_student_semester (student_id, semester_id),
    -- 唯一约束：防止同一学生同一天同一事件重复录入
    UNIQUE KEY uk_student_event_date (student_id, event_id, record_date, semester_id)
);

-- 校级事件记录（含唯一约束）
CREATE TABLE student_school_record (
    record_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    event_id INT NOT NULL,
    semester_id INT NOT NULL,
    get_date DATE NOT NULL,
    class_id INT NOT NULL COMMENT '记录时所属班级',
    grade_id INT NOT NULL COMMENT '记录时所属级号',
    score INT,
    proof VARCHAR(255) COMMENT '证书/文件编号',
    is_deleted TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (event_id) REFERENCES school_event_type(event_id),
    -- 唯一约束：荣誉证书编号唯一
    UNIQUE KEY uk_proof (proof)
);

-- 学生任务记录（以学年为单位）
CREATE TABLE student_task_finish (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    task_id INT NOT NULL COMMENT '任务ID',
    year_id INT NOT NULL COMMENT '当前所属学年',

    -- 原始信息
    original_task_id INT COMMENT '原始任务ID',
    original_year_id INT COMMENT '原始学年',

    -- 结转信息
    is_carried_over TINYINT DEFAULT 0 COMMENT '是否为结转任务',
    carryover_count INT DEFAULT 0 COMMENT '已结转次数',
    current_score DECIMAL(6,2) COMMENT '当前可得分数',

    -- 完成信息
    status TINYINT DEFAULT 0 COMMENT '0=未完成 1=已完成 2=已作废',
    finish_date DATE,
    finish_year_id INT COMMENT '完成时学年',
    proof TEXT,

    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW(),

    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (task_id) REFERENCES grade_moral_task(task_id),
    FOREIGN KEY (year_id) REFERENCES school_year(year_id),

    UNIQUE KEY uk_student_task_year (student_id, task_id, year_id)
);

-- 处分记录（独立表，便于权限控制）
CREATE TABLE punishment_record (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    event_id INT NOT NULL COMMENT '处分事件类型',
    semester_id INT NOT NULL,
    punishment_date DATE NOT NULL,
    class_id INT NOT NULL,
    grade_id INT NOT NULL,
    score_deduct INT NOT NULL COMMENT '扣分',
    level VARCHAR(20) COMMENT '警告/严重警告/记过/留校察看',
    reason TEXT,
    recorder VARCHAR(20),
    is_revoked TINYINT DEFAULT 0 COMMENT '是否已撤销',
    revoke_date DATE,
    revoke_by VARCHAR(20),
    revoke_reason TEXT,
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (event_id) REFERENCES school_event_type(event_id)
);

-- 集体事件表
CREATE TABLE collective_event (
    event_id INT PRIMARY KEY AUTO_INCREMENT,
    event_name VARCHAR(100) NOT NULL COMMENT '事件名称',
    event_type VARCHAR(20) NOT NULL COMMENT '班级荣誉/集体活动/集体违纪',
    semester_id INT NOT NULL COMMENT '所属学期',
    event_date DATE NOT NULL COMMENT '事件日期',
    score INT NOT NULL COMMENT '每人得分/扣分',
    description TEXT COMMENT '事件描述',
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (semester_id) REFERENCES semester(semester_id)
);

-- 集体事件分配记录
CREATE TABLE collective_event_distribution (
    id INT PRIMARY KEY AUTO_INCREMENT,
    event_id INT NOT NULL COMMENT '集体事件ID',
    student_id VARCHAR(20) NOT NULL COMMENT '学生ID',
    class_id INT NOT NULL COMMENT '学生所属班级',
    score_assigned INT COMMENT '分配分数',
    is_participant TINYINT DEFAULT 1 COMMENT '是否实际参与',
    remark TEXT COMMENT '备注',
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (event_id) REFERENCES collective_event(event_id),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (class_id) REFERENCES class(class_id),
    UNIQUE KEY uk_event_student (event_id, student_id)
);
```

### 3.7 系统配置表

```sql
-- 学期结转配置
CREATE TABLE semester_carryover_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    data_type VARCHAR(20) NOT NULL COMMENT 'honor/punishment/task',
    can_carryover TINYINT DEFAULT 1,
    score_factor DECIMAL(3,2) DEFAULT 1.00 COMMENT '结转分值系数',
    description TEXT
);

INSERT INTO semester_carryover_config (data_type, can_carryover, score_factor, description) VALUES
('honor', 0, 1.00, '荣誉不结转，记录保留在原学期'),
('punishment', 0, 1.00, '处分不结转，记录保留在原学期'),
('task_unfinished', 1, 0.60, '未完成任务可结转，每次×60%');

-- 数据可见性配置
CREATE TABLE data_visibility_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    data_type VARCHAR(50) NOT NULL,
    visible_roles JSON NOT NULL COMMENT '["admin", "xuefa"]',
    description TEXT
);

INSERT INTO data_visibility_config (data_type, visible_roles, description) VALUES
('punishment_record', '["admin", "xuefa"]', '处分记录'),
('daily_negative', '["admin", "xuefa", "cleader"]', '消极事件'),
('daily_positive', '["admin", "xuefa", "cleader", "teacher"]', '积极事件'),
('honor_record', '["admin", "xuefa", "cleader", "teacher", "student", "parent"]', '荣誉记录'),
('evaluation_score', '["admin", "xuefa", "cleader", "student", "parent"]', '评价得分');

-- 预警配置
CREATE TABLE warning_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    rule_name VARCHAR(50) NOT NULL,
    trigger_type VARCHAR(20) NOT NULL COMMENT 'score_threshold/count_threshold',
    trigger_value INT NOT NULL,
    notify_roles JSON COMMENT '["cleader", "xuefa"]',
    is_active TINYINT DEFAULT 1
);

INSERT INTO warning_config (rule_name, trigger_type, trigger_value, notify_roles) VALUES
('德育分过低', 'score_threshold', 50, '["cleader", "xuefa"]'),
('扣分过多', 'score_threshold', -20, '["cleader", "xuefa"]'),
('违纪次数过多', 'count_threshold', 5, '["cleader", "xuefa"]');
```

### 3.8 评价与日志表

```sql
-- 德育评价
CREATE TABLE moral_evaluation (
    eval_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    semester_id INT NOT NULL,
    class_id INT NOT NULL COMMENT '评价时所属班级',
    grade_id INT NOT NULL COMMENT '评价时所属级号',
    total_score DECIMAL(6,2) DEFAULT 0 COMMENT '总分（可超100）',
    level VARCHAR(10) COMMENT '优秀/良好/合格/不合格',
    update_time DATETIME DEFAULT NOW(),
    UNIQUE KEY uk_student_semester (student_id, semester_id),
    FOREIGN KEY (student_id) REFERENCES student(student_id)
);

-- 任务结转日志
CREATE TABLE task_carryover_log (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    original_task_id INT NOT NULL COMMENT '原始任务ID',
    from_year_id INT NOT NULL,
    to_year_id INT NOT NULL,
    carryover_index INT NOT NULL COMMENT '第几次结转',
    score_before DECIMAL(6,2),
    score_after DECIMAL(6,2),
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    FOREIGN KEY (original_task_id) REFERENCES grade_moral_task(task_id),
    FOREIGN KEY (from_year_id) REFERENCES school_year(year_id),
    FOREIGN KEY (to_year_id) REFERENCES school_year(year_id)
);

-- 预警日志
CREATE TABLE warning_log (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    rule_id INT NOT NULL,
    semester_id INT NOT NULL,
    warning_level VARCHAR(20) DEFAULT 'warning',
    message TEXT,
    is_read TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT NOW()
);

-- 操作日志
CREATE TABLE moral_operation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    operator VARCHAR(20) NOT NULL,
    operator_role VARCHAR(20),
    operation VARCHAR(20) NOT NULL COMMENT 'INSERT/UPDATE/DELETE/REVOKE',
    table_name VARCHAR(50) NOT NULL,
    record_id INT,
    semester_id INT,
    old_data JSON,
    new_data JSON,
    reason TEXT,
    ip_address VARCHAR(50),
    created_at DATETIME DEFAULT NOW(),
    INDEX idx_operator (operator),
    INDEX idx_table_record (table_name, record_id)
);
```

---

## 四、核心业务流程

### 4.1 学生班级变更流程

```
┌─────────────────────────────────────────────────────────────┐
│                    班级变更处理流程                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 验证变更类型和权限                                       │
│       ↓                                                     │
│  2. 结束旧班级履历（设置 end_date）                          │
│       ↓                                                     │
│  3. 创建新班级履历（除转出/毕业/休学外）                      │
│       ↓                                                     │
│  4. 更新学生表（status、grade_id 如需要）                    │
│       ↓                                                     │
│  5. 记录操作日志                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 德育记录录入流程

```
┌─────────────────────────────────────────────────────────────┐
│                    记录录入流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 验证录入权限                                            │
│       ↓                                                     │
│  2. 获取学生当前班级履历                                    │
│       ↓                                                     │
│  3. 检查累进规则（消极事件）                                 │
│       ↓                                                     │
│  4. 计算实际得分/扣分                                       │
│       ↓                                                     │
│  5. 插入记录（包含班级/级号快照）                            │
│       ↓                                                     │
│  6. 检查预警阈值                                            │
│       ↓                                                     │
│  7. 记录操作日志                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 德育总分计算流程

```
┌─────────────────────────────────────────────────────────────┐
│                    总分计算流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 获取基础分（60分）                                       │
│       ↓                                                     │
│  2. 计算日常表现分 = SUM(积极事件) + SUM(消极事件)           │
│       ↓                                                     │
│  3. 计算校级事件分 = SUM(荣誉) + SUM(处分)                   │
│       ↓                                                     │
│  4. 计算任务完成分 = SUM(已完成任务分数)                     │
│       ↓                                                     │
│  5. 计算总分 = 基础分 + 日常分 + 校级分 + 任务分             │
│       ↓                                                     │
│  6. 判定等级（≥90优秀，≥80良好，≥60合格，<60不合格）         │
│       ↓                                                     │
│  7. 保存评价记录                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 学年任务结转流程

```
┌─────────────────────────────────────────────────────────────┐
│                    学年任务结转流程                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  学年末执行：                                               │
│                                                             │
│  1. 查询所有未完成任务                                      │
│       ↓                                                     │
│  2. 检查是否超过最大结转次数                                 │
│       ├── 是 → 标记任务作废                                 │
│       └── 否 → 继续                                         │
│       ↓                                                     │
│  3. 计算结转后分值 = 当前分值 × 60%                          │
│       ↓                                                     │
│  4. 记录结转日志                                            │
│       ↓                                                     │
│  5. 更新任务所属学年                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、核心查询

### 5.1 学生当前班级

```sql
SELECT sch.*, c.class_name, g.grade_name
FROM student_class_history sch
JOIN class c ON sch.class_id = c.class_id
JOIN grade g ON sch.grade_id = g.grade_id
WHERE sch.student_id = ? AND sch.end_date IS NULL;
```

### 5.2 学生当前年级等级

```sql
SELECT
    s.student_id, s.name,
    g.grade_name, g.enrollment_year,
    YEAR(CURDATE()) - g.enrollment_year AS years_after_enrollment,
    glc.level_name AS current_level
FROM student s
JOIN grade g ON s.grade_id = g.grade_id
JOIN grade_level_config glc ON glc.years_after_enrollment = YEAR(CURDATE()) - g.enrollment_year
WHERE s.student_id = ?;
```

### 5.3 学生德育总分历史

```sql
SELECT
    me.semester_id, sem.semester_name,
    me.total_score, me.level,
    c.class_name, g.grade_name
FROM moral_evaluation me
JOIN semester sem ON me.semester_id = sem.semester_id
JOIN class c ON me.class_id = c.class_id
JOIN grade g ON me.grade_id = g.grade_id
WHERE me.student_id = ?
ORDER BY sem.start_date;
```

### 5.4 班级学期统计

```sql
SELECT
    c.class_name,
    COUNT(*) AS student_count,
    AVG(me.total_score) AS avg_score,
    SUM(CASE WHEN me.level = '优秀' THEN 1 ELSE 0 END) AS excellent_count,
    SUM(CASE WHEN me.level = '不合格' THEN 1 ELSE 0 END) AS fail_count
FROM moral_evaluation me
JOIN class c ON me.class_id = c.class_id
WHERE me.semester_id = ? AND c.class_id = ?
GROUP BY c.class_id;
```

### 5.5 学生班级履历

```sql
SELECT
    sch.start_date, sch.end_date, sch.change_reason,
    c.class_name, g.grade_name
FROM student_class_history sch
JOIN class c ON sch.class_id = c.class_id
JOIN grade g ON sch.grade_id = g.grade_id
WHERE sch.student_id = ?
ORDER BY sch.start_date;
```

### 5.6 任务结转历史

```sql
SELECT
    tcl.carryover_index AS '结转次数',
    sy_from.year_name AS '原学年',
    sy_to.year_name AS '目标学年',
    tcl.score_before AS '结转前分值',
    tcl.score_after AS '结转后分值',
    tcl.created_at AS '结转时间'
FROM task_carryover_log tcl
JOIN school_year sy_from ON tcl.from_year_id = sy_from.year_id
JOIN school_year sy_to ON tcl.to_year_id = sy_to.year_id
WHERE tcl.student_id = ? AND tcl.original_task_id = ?
ORDER BY tcl.carryover_index;
```

### 5.7 年级学期统计

```sql
SELECT
    g.grade_name,
    COUNT(DISTINCT me.student_id) AS student_count,
    AVG(me.total_score) AS avg_score,
    MAX(me.total_score) AS max_score,
    MIN(me.total_score) AS min_score,
    SUM(CASE WHEN me.level = '优秀' THEN 1 ELSE 0 END) AS excellent_count,
    SUM(CASE WHEN me.level = '良好' THEN 1 ELSE 0 END) AS good_count,
    SUM(CASE WHEN me.level = '合格' THEN 1 ELSE 0 END) AS pass_count,
    SUM(CASE WHEN me.level = '不合格' THEN 1 ELSE 0 END) AS fail_count
FROM moral_evaluation me
JOIN grade g ON me.grade_id = g.grade_id
WHERE me.semester_id = ?
GROUP BY g.grade_id
ORDER BY g.enrollment_year DESC;
```

### 5.8 学生德育趋势分析

```sql
-- 学生各学期得分趋势
SELECT
    s.student_id,
    s.name,
    sem.semester_name,
    me.total_score,
    me.level,
    RANK() OVER (PARTITION BY sem.semester_id ORDER BY me.total_score DESC) AS grade_rank
FROM student s
JOIN moral_evaluation me ON s.student_id = me.student_id
JOIN semester sem ON me.semester_id = sem.semester_id
WHERE s.student_id = ?
ORDER BY sem.start_date;

-- 学生月度得分变化（当前学期）
SELECT
    DATE_FORMAT(dr.record_date, '%Y-%m') AS month,
    SUM(CASE WHEN de.event_type = 1 THEN de.score ELSE 0 END) AS positive_score,
    SUM(CASE WHEN de.event_type = 2 THEN ABS(de.score) ELSE 0 END) AS negative_score,
    COUNT(*) AS event_count
FROM student_daily_record dr
JOIN daily_event_type de ON dr.event_id = de.event_id
WHERE dr.student_id = ? AND dr.semester_id = ? AND dr.is_deleted = 0
GROUP BY DATE_FORMAT(dr.record_date, '%Y-%m')
ORDER BY month;
```

### 5.9 班级学期对比

```sql
-- 班级各学期统计对比
SELECT
    c.class_name,
    sem.semester_name,
    COUNT(*) AS student_count,
    AVG(me.total_score) AS avg_score,
    SUM(CASE WHEN me.level = '优秀' THEN 1 ELSE 0 END) AS excellent_count,
    ROUND(SUM(CASE WHEN me.level = '优秀' THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 1) AS excellent_rate
FROM moral_evaluation me
JOIN class c ON me.class_id = c.class_id
JOIN semester sem ON me.semester_id = sem.semester_id
WHERE c.class_id = ?
GROUP BY sem.semester_id
ORDER BY sem.start_date;
```

### 5.10 集体事件查询

```sql
-- 班级集体事件列表
SELECT
    ce.event_name,
    ce.event_type,
    ce.event_date,
    ce.score,
    COUNT(ced.id) AS participant_count
FROM collective_event ce
LEFT JOIN collective_event_distribution ced ON ce.event_id = ced.event_id
WHERE ce.semester_id = ?
GROUP BY ce.event_id
ORDER BY ce.event_date DESC;

-- 学生集体事件得分汇总
SELECT
    s.student_id,
    s.name,
    SUM(ced.score_assigned) AS total_collective_score
FROM student s
JOIN collective_event_distribution ced ON s.student_id = ced.student_id
JOIN collective_event ce ON ced.event_id = ce.event_id
WHERE ce.semester_id = ? AND s.class_id = ?
GROUP BY s.student_id;
```

---

## 六、API 权限配置

```yaml
# 德育评价系统 API 权限配置

# 基础数据录入
/daily-records:
  allowed_roles: [cleader, teacher, xuefa, admin]
  min_level: 0

/daily-records/{id}:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

# 批量导入
/daily-records/import:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

/daily-records/template:
  allowed_roles: [cleader, teacher, xuefa, admin]
  min_level: 0

/school-records:
  allowed_roles: [xuefa, admin]
  min_level: 5

/task-finish:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

# 集体事件
/collective-events:
  allowed_roles: [xuefa, admin]
  min_level: 5

/collective-events/{id}:
  allowed_roles: [xuefa, admin]
  min_level: 5

/collective-events/{id}/distribute:
  allowed_roles: [xuefa, admin]
  min_level: 5

# 处分管理
/punishment-records:
  allowed_roles: [xuefa, admin]
  min_level: 5

/punishment-records/{id}/revoke:
  allowed_roles: [xuefa, admin]
  min_level: 10

# 班级变更
/students/{id}/class-change:
  allowed_roles: [xuefa, admin]
  min_level: 10

# 任务结转
/tasks/carryover:
  allowed_roles: [xuefa, admin]
  min_level: 10

# 评价查询
/evaluation/student/{id}:
  allowed_roles: [cleader, xuefa, admin, student, parent]
  min_level: 0

/evaluation/class/{id}:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

/evaluation/grade/{id}:
  allowed_roles: [xuefa, admin]
  min_level: 5

# 趋势分析
/analysis/student/{id}/trend:
  allowed_roles: [cleader, xuefa, admin, student, parent]
  min_level: 0

/analysis/class/{id}/compare:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

/analysis/grade/{id}/ranking:
  allowed_roles: [xuefa, admin]
  min_level: 5

# 数据展示
/reports/student/{id}:
  allowed_roles: [cleader, xuefa, admin, student, parent]
  min_level: 0

/reports/class/{id}:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

/reports/grade/{id}:
  allowed_roles: [xuefa, admin]
  min_level: 5

/reports/export/student/{id}:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

/reports/export/class/{id}:
  allowed_roles: [cleader, xuefa, admin]
  min_level: 0

# 系统配置
/carryover-config:
  allowed_roles: [admin]
  min_level: 100

/visibility-config:
  allowed_roles: [admin]
  min_level: 100
```

---

## 七、批量导入功能设计

### 7.1 导入模板

**日常表现导入模板（Excel）**

| 学号 | 学生姓名 | 事件名称 | 事件日期 | 备注 |
|------|---------|---------|---------|------|
| 2025001 | 张三 | 拾金不昧 | 2025-10-15 | 捡到钱包交公 |
| 2025002 | 李四 | 迟到 | 2025-10-16 | 早上迟到 |

**校级事件导入模板（Excel）**

| 学号 | 学生姓名 | 事件名称 | 事件级别 | 获得日期 | 证书编号 | 备注 |
|------|---------|---------|---------|---------|---------|------|
| 2025001 | 张三 | 三好学生 | 校级 | 2025-12-01 | CERT001 | |
| 2025002 | 李四 | 警告 | 校级 | 2025-11-20 | WARN001 | 违纪 |

### 7.2 导入流程

```
┌─────────────────────────────────────────────────────────────┐
│                    批量导入流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 下载导入模板                                            │
│       ↓                                                     │
│  2. 填写数据（按模板格式）                                   │
│       ↓                                                     │
│  3. 上传Excel文件                                           │
│       ↓                                                     │
│  4. 系统校验                                                │
│       ├── 学号是否存在                                      │
│       ├── 事件名称是否存在                                  │
│       ├── 日期格式是否正确                                  │
│       ├── 是否重复录入                                      │
│       └── 分数范围是否正确                                  │
│       ↓                                                     │
│  5. 返回校验结果                                            │
│       ├── 成功条数                                          │
│       ├── 失败条数                                          │
│       └── 失败原因列表                                      │
│       ↓                                                     │
│  6. 用户确认导入                                            │
│       ↓                                                     │
│  7. 批量插入数据库                                          │
│       ↓                                                     │
│  8. 返回导入结果                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 校验规则

| 校验项 | 规则 | 错误提示 |
|-------|------|---------|
| 学号 | 必须存在 | 第N行：学号XXX不存在 |
| 学生姓名 | 与学号匹配 | 第N行：学号与姓名不匹配 |
| 事件名称 | 必须存在 | 第N行：事件名称XXX不存在 |
| 事件日期 | 有效日期格式 | 第N行：日期格式错误 |
| 事件日期 | 不能是未来 | 第N行：日期不能是未来时间 |
| 重复检查 | 同一学生同一天同一事件 | 第N行：记录已存在 |
| 证书编号 | 校级事件必填 | 第N行：校级事件必须填写证书编号 |
| 证书编号 | 唯一性检查 | 第N行：证书编号已存在 |

### 7.4 API设计

```python
@router.post("/daily-records/import")
async def import_daily_records(
    file: UploadFile,
    semester_id: int,
    current_user: User = Depends(get_current_user)
):
    """
    批量导入日常表现记录
    """
    # 权限检查
    if not _user_has_any_role(current_user, ["cleader", "xuefa", "admin"]):
        raise HTTPException(403, "无权限")

    # 读取Excel
    df = pd.read_excel(file.file)

    errors = []
    success_count = 0

    for idx, row in df.iterrows():
        line_num = idx + 2  # Excel行号从2开始（第1行是表头）

        # 校验学生
        student = db.query("SELECT * FROM student WHERE student_id = ?", row['学号']).first()
        if not student:
            errors.append(f"第{line_num}行：学号不存在")
            continue
        if student['name'] != row['学生姓名']:
            errors.append(f"第{line_num}行：学号与姓名不匹配")
            continue

        # 校验事件
        event = db.query("SELECT * FROM daily_event_type WHERE event_name = ?", row['事件名称']).first()
        if not event:
            errors.append(f"第{line_num}行：事件名称不存在")
            continue

        # 校验日期
        try:
            record_date = pd.to_datetime(row['事件日期']).date()
            if record_date > date.today():
                errors.append(f"第{line_num}行：日期不能是未来时间")
                continue
        except:
            errors.append(f"第{line_num}行：日期格式错误")
            continue

        # 检查重复
        exists = db.query("""
            SELECT * FROM student_daily_record
            WHERE student_id = ? AND event_id = ? AND record_date = ? AND semester_id = ?
        """, row['学号'], event['event_id'], record_date, semester_id).first()
        if exists:
            errors.append(f"第{line_num}行：该学生当天已有此事件记录")
            continue

        # 插入记录
        # ... 省略插入逻辑
        success_count += 1

    return {
        "success_count": success_count,
        "error_count": len(errors),
        "errors": errors[:20]  # 最多返回20条错误
    }

@router.get("/daily-records/template")
async def download_import_template():
    """
    下载日常表现导入模板
    """
    # 返回Excel模板文件
    pass
```

---

## 八、完整数据示例

### 8.1 学生德育得分示例

```
学生A某学期：

基础分：60

积极事件：
- 拾金不昧：+3分
- 助人为乐：+5分
- 按时出勤：+2分
小计：+10分

消极事件：
- 迟到：-2分
- 迟到（第2次，累进+1）：-3分
小计：-5分

校级荣誉：
- 三好学生：+10分
小计：+10分

任务完成：
- 志愿服务：+20分
- 社会实践：+30分
小计：+50分

总分 = 60 + 10 - 5 + 10 + 50 = 125分
等级 = 优秀
```

### 8.2 任务结转完整示例

```
学生A（2025级）任务情况：

高一学年（2025-2026）：
┌─────────────────────────────────────────────────────────────────┐
│ 任务名称      │ 原分值 │ 状态   │ 处理                          │
├─────────────────────────────────────────────────────────────────┤
│ 志愿服务      │ 20分   │ 已完成 │ 得20分，计入高一学年          │
│ 社会实践      │ 30分   │ 已完成 │ 得30分，计入高一学年          │
│ 技能考核A     │ 30分   │ 未完成 │ 结转到高二，分值变为18分      │
│ 技能考核B     │ 25分   │ 未完成 │ 结转到高二，分值变为15分      │
└─────────────────────────────────────────────────────────────────┘

高二学年（2026-2027）：
┌─────────────────────────────────────────────────────────────────┐
│ 任务名称      │ 当前分值 │ 结转次数 │ 状态   │ 处理            │
├─────────────────────────────────────────────────────────────────┤
│ 技能考核A     │ 18分    │ 1        │ 未完成 │ 结转到高三      │
│ 技能考核B     │ 15分    │ 1        │ 已完成 │ 得15分          │
│ 新任务C       │ 20分    │ 0        │ 未完成 │ 结转到高三      │
└─────────────────────────────────────────────────────────────────┘

高三学年（2027-2028）：
┌─────────────────────────────────────────────────────────────────┐
│ 任务名称      │ 当前分值 │ 结转次数 │ 状态   │ 处理            │
├─────────────────────────────────────────────────────────────────┤
│ 技能考核A     │ 10.8分  │ 2        │ 已完成 │ 得10.8分        │
│ 新任务C       │ 12分    │ 1        │ 未完成 │ 毕业，任务作废  │
└─────────────────────────────────────────────────────────────────┘

各学年任务得分：
- 高一学年：20 + 30 = 50分
- 高二学年：15分
- 高三学年：10.8分
```

---

## 九、系统初始化与数据导入

### 9.1 数据导入清单

| 数据类型 | 来源 | 优先级 | 依赖 |
|---------|------|-------|------|
| 学年配置 | 系统预置 | P0 | 无 |
| 级号配置 | 系统预置 | P0 | 无 |
| 学期配置 | 系统预置 | P0 | 学年 |
| 班级信息 | Excel导入 | P0 | 级号 |
| 学生信息 | Excel导入 | P0 | 班级、级号 |
| 事件类型配置 | 系统界面配置 | P1 | 无 |

**说明：系统从上线后开始记录新数据，无需导入历史德育记录**

### 9.2 导入依赖关系

```
┌─────────────────────────────────────────────────────────────┐
│                    数据导入依赖关系                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  第一层（无依赖）                                            │
│  ├── 学年配置                                              │
│  └── 级号配置                                              │
│           ↓                                                 │
│  第二层（依赖第一层）                                        │
│  ├── 学期配置 ← 依赖：学年                                  │
│  └── 班级信息 ← 依赖：级号                                  │
│           ↓                                                 │
│  第三层（依赖第二层）                                        │
│  └── 学生信息 ← 依赖：级号、班级                            │
│                                                             │
│  独立配置（随时可配置）                                      │
│  └── 事件类型配置                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 9.3 导入步骤详情

#### 步骤1：学年配置

```
系统预置数据：

| 学年名称 | 开始日期 | 结束日期 | 是否当前学年 |
|---------|---------|---------|-------------|
| 2023-2024学年 | 2023-09-01 | 2024-07-15 | 否 |
| 2024-2025学年 | 2024-09-01 | 2025-07-15 | 否 |
| 2025-2026学年 | 2025-09-01 | 2026-07-15 | 是 |
```

#### 步骤2：级号配置

```
系统根据学年自动生成：

| 级号名称 | 入学年份 | 当前年级等级 |
|---------|---------|-------------|
| 2023级 | 2023 | 高三 |
| 2024级 | 2024 | 高二 |
| 2025级 | 2025 | 高一 |
```

#### 步骤3：学期配置

```
依赖：学年配置 ✅

| 学期名称 | 所属学年 | 开始日期 | 结束日期 | 状态 |
|---------|---------|---------|---------|------|
| 2023-2024上 | 2023-2024学年 | 2023-09-01 | 2024-01-20 | 已结束 |
| 2023-2024下 | 2023-2024学年 | 2024-02-20 | 2024-07-15 | 已结束 |
| 2024-2025上 | 2024-2025学年 | 2024-09-01 | 2025-01-20 | 已结束 |
| 2024-2025下 | 2024-2025学年 | 2025-02-20 | 2025-07-15 | 已结束 |
| 2025-2026上 | 2025-2026学年 | 2025-09-01 | 2026-01-20 | 当前学期 |
| 2025-2026下 | 2025-2026学年 | 2026-02-20 | 2026-07-15 | 未开始 |
```

#### 步骤4：班级信息导入

```
依赖：级号配置 ✅

导入模板（Excel）：

| 级号 | 班号 | 班主任工号 |
|------|------|-----------|
| 2023级 | 1 | T001 |
| 2023级 | 2 | T002 |
| 2024级 | 1 | T003 |
| ... | ... | ... |
```

#### 步骤5：学生信息导入

```
依赖检查：
  ✅ 级号配置已完成
  ⚠️ 班级信息未完成 → [立即导入]

导入模板（Excel）：

| 学号 | 姓名 | 性别 | 级号 | 班号 | 入学日期 |
|------|------|------|------|------|---------|
| 2023001 | 张三 | 男 | 2023级 | 1 | 2023-09-01 |
| 2023002 | 李四 | 女 | 2023级 | 1 | 2023-09-01 |
| ... | ... | ... | ... | ... | ... |

导入后自动处理：
1. 创建学生记录
2. 创建班级履历（入学记录）
3. 验证数据完整性
4. 生成导入报告
```

### 9.4 导入依赖提示界面

```
┌─────────────────────────────────────────────────────────────┐
│  数据导入向导                                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  步骤进度：✅学年配置→✅级号配置→✅学期配置→✅班级信息→●学生信息│
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ⚠️ 导入"学生信息"前，请确保以下数据已完成导入：            │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 序号 │ 依赖数据  │ 状态    │ 操作                   │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  1   │ 级号配置  │ ✅ 已完成 │ [查看]                │   │
│  │  2   │ 班级信息  │ ❌ 未导入 │ [立即导入]            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  💡 提示：学生需要关联所属级号和班级，请先完成上述配置。    │
│                                                             │
│  [上一步]  [暂时跳过]  [继续导入]                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 9.5 导入依赖检查API

```python
class ImportDependencyChecker:
    """导入依赖检查器"""

    DATA_STATUS = {
        'school_year': {'name': '学年配置', 'requires': []},
        'grade': {'name': '级号配置', 'requires': []},
        'semester': {'name': '学期配置', 'requires': ['school_year']},
        'class': {'name': '班级信息', 'requires': ['grade']},
        'student': {'name': '学生信息', 'requires': ['grade', 'class']},
    }

    @classmethod
    def get_all_status(cls, db) -> dict:
        """获取所有数据导入状态"""
        result = {}
        for key, info in cls.DATA_STATUS.items():
            table_map = {
                'school_year': 'school_year',
                'grade': 'grade',
                'semester': 'semester',
                'class': 'class',
                'student': 'student'
            }
            count = db.query(f"SELECT COUNT(*) as cnt FROM {table_map[key]}").first()['cnt']
            result[key] = {
                'name': info['name'],
                'completed': count > 0,
                'count': count
            }
        return result

    @classmethod
    def check_dependency(cls, db, data_type: str) -> dict:
        """检查某个数据类型的依赖状态"""
        if data_type not in cls.DATA_STATUS:
            return {'can_import': True, 'missing': []}

        dep_info = cls.DATA_STATUS[data_type]
        missing = []

        for req in dep_info['requires']:
            count = db.query(f"SELECT COUNT(*) as cnt FROM {req}").first()['cnt']
            if count == 0:
                missing.append({
                    'key': req,
                    'name': cls.DATA_STATUS[req]['name']
                })

        return {
            'can_import': len(missing) == 0,
            'missing': missing
        }


@router.get("/import/status")
async def get_import_status():
    """获取所有数据导入状态"""
    return ImportDependencyChecker.get_all_status(db)


@router.get("/import/check-dependency/{data_type}")
async def check_import_dependency(data_type: str):
    """检查数据导入依赖"""
    return ImportDependencyChecker.check_dependency(db, data_type)
```

---

## 十、事件类型配置管理

### 10.1 管理权限

| 操作 | 角色权限 | 说明 |
|------|---------|------|
| 查看事件类型 | 全员可见 | 所有登录用户可查看 |
| 添加事件类型 | 学发部、管理员 | 配置分值、累进规则 |
| 编辑事件类型 | 管理员 | 修改分值、名称 |
| 禁用事件类型 | 管理员 | 已使用的事件不可删除，只能禁用 |

### 10.2 分值范围约束

| 事件类型 | 分值范围 | 约束 |
|---------|---------|------|
| 日常积极事件 | +1 ~ +5 | 必须大于0 |
| 日常消极事件 | -1 ~ -5 | 必须小于0 |
| 校级荣誉 | +5 ~ +20 | 必须大于0 |
| 校级处分 | -5 ~ -30 | 必须小于0 |

### 10.3 事件类型配置界面

```
┌─────────────────────────────────────────────────────────────┐
│  事件类型配置                                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  类型筛选：[全部] [日常积极] [日常消极] [校级荣誉] [校级处分]│
│                                                             │
│  [+ 添加事件类型]                                           │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  日常积极事件                                               │
│  ┌─────┬────────────┬────────┬────────┬────────┐           │
│  │ 序号│ 事件名称    │ 分值   │ 状态   │ 操作   │           │
│  ├─────┼────────────┼────────┼────────┼────────┤           │
│  │  1  │ 拾金不昧    │ +3     │ 启用   │ 编辑   │           │
│  │  2  │ 助人为乐    │ +3     │ 启用   │ 编辑   │           │
│  │  3  │ 按时出勤    │ +1     │ 启用   │ 编辑   │           │
│  └─────┴────────────┴────────┴────────┴────────┘           │
│                                                             │
│  日常消极事件（可配置累进规则）                              │
│  ┌─────┬────────────┬────────┬─────────┬────────┐          │
│  │ 序号│ 事件名称    │ 分值   │ 累进规则│ 操作   │          │
│  ├─────┼────────────┼────────┼─────────┼────────┤          │
│  │  1  │ 迟到        │ -2     │ 已配置  │ 编辑   │          │
│  │  2  │ 早退        │ -2     │ 未配置  │ 编辑   │          │
│  └─────┴────────────┴────────┴─────────┴────────┘          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 10.4 添加事件类型界面

```
┌─────────────────────────────────────────────────────────────┐
│  添加事件类型                                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  事件类别：○ 日常积极  ○ 日常消极  ○ 校级荣誉  ○ 校级处分   │
│                                                             │
│  事件名称：[                                    ] 必填       │
│                                                             │
│  事件级别：[下拉：校级/市级/省级/国家级] （校级事件必填）   │
│                                                             │
│  分值：    [    ] 分                                        │
│            💡 积极事件输入正数，消极事件输入负数            │
│                                                             │
│  累进规则：（仅消极事件可配置）                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ □ 启用累进加重                                      │   │
│  │   时间窗口：[90] 天内                                │   │
│  │   第2次，额外扣 [1] 分                               │   │
│  │   第3次，额外扣 [3] 分                               │   │
│  │   第4次及以上，额外扣 [5] 分                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│           [取消]  [保存]                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 10.5 事件类型管理API

```python
@router.get("/event-types")
async def get_event_types(
    event_type: str = None,
    is_active: int = None
):
    """获取事件类型列表（全员可查看）"""
    # ...

@router.post("/event-types/daily")
async def create_daily_event_type(
    event_name: str,
    event_type: int,  # 1=积极, 2=消极
    score: int,
    description: str = None,
    escalation_rules: dict = None,
    current_user: User = Depends(get_current_user)
):
    """
    添加日常事件类型（学发部、管理员）
    权限检查、分值校验、名称去重
    """
    if not _user_has_any_role(current_user, ["xuefa", "admin"]):
        raise HTTPException(403, "需要学发部或管理员权限")

    # 分值校验
    if event_type == 1 and (score <= 0 or score > 5):
        raise HTTPException(400, "日常积极事件分值范围：+1~+5")
    if event_type == 2 and (score >= 0 or score < -5):
        raise HTTPException(400, "日常消极事件分值范围：-1~-5")

    # ...

@router.post("/event-types/school")
async def create_school_event_type(
    event_name: str,
    event_type: int,  # 1=荣誉, 2=处分
    event_level: str,
    score: int,
    current_user: User = Depends(get_current_user)
):
    """
    添加校级事件类型（学发部、管理员）
    """
    if not _user_has_any_role(current_user, ["xuefa", "admin"]):
        raise HTTPException(403, "需要学发部或管理员权限")

    # 分值校验
    if event_type == 1 and (score <= 0 or score > 20):
        raise HTTPException(400, "校级荣誉分值范围：+5~+20")
    if event_type == 2 and (score >= 0 or score < -30):
        raise HTTPException(400, "校级处分分值范围：-5~-30")

    # ...

@router.put("/event-types/daily/{event_id}")
async def update_daily_event_type(event_id: int, ...):
    """编辑事件类型（仅管理员）"""
    # 已使用的事件不可修改分值
    # ...

@router.delete("/event-types/daily/{event_id}")
async def delete_daily_event_type(event_id: int, ...):
    """删除/禁用事件类型（仅管理员）"""
    # 已使用的事件只能禁用，不能删除
    # ...
```

---

## 十一、数据验证方案

### 11.1 验证检查项

| 检查项 | 检查内容 | 严重程度 | 说明 |
|-------|---------|---------|------|
| 基础数据完整性 | 学年、级号、班级、学生数量 | 错误 | 系统运行必要数据 |
| 外键关联完整性 | 学生→级号、履历→班级 | 错误 | 数据关联正确性 |
| 班级履历完整性 | 每个在校学生都有当前班级 | 错误 | 状态一致性 |
| 数据状态一致性 | 学生状态与履历匹配 | 警告 | 可能存在问题 |
| 事件类型完整性 | 必要的事件类型已配置 | 警告 | 影响录入功能 |

### 11.2 验证脚本

```python
def validate_data(db):
    """数据验证"""

    errors = []
    warnings = []

    print("=" * 60)
    print("德育评价系统数据验证")
    print("=" * 60)

    # 1. 基础数据数量检查
    print("\n[1] 基础数据检查")

    checks = [
        ("学年", "school_year", 3),
        ("级号", "grade", 3),
        ("学期", "semester", 6),
    ]

    for name, table, expected in checks:
        count = db.query(f"SELECT COUNT(*) as cnt FROM {table}").first()['cnt']
        status = "✅" if count >= expected else "⚠️"
        print(f"  {status} {name}: {count} 条")
        if count < expected:
            warnings.append(f"{name}数据不足")

    class_count = db.query("SELECT COUNT(*) as cnt FROM class").first()['cnt']
    student_count = db.query("SELECT COUNT(*) as cnt FROM student").first()['cnt']
    print(f"  {'✅' if class_count > 0 else '❌'} 班级: {class_count} 个")
    print(f"  {'✅' if student_count > 0 else '❌'} 学生: {student_count} 人")

    if class_count == 0:
        errors.append("班级数据为空")
    if student_count == 0:
        errors.append("学生数据为空")

    # 2. 外键关联检查
    print("\n[2] 外键关联检查")

    orphan = db.query("""
        SELECT COUNT(*) as cnt FROM student s
        LEFT JOIN grade g ON s.grade_id = g.grade_id
        WHERE g.grade_id IS NULL
    """).first()['cnt']
    print(f"  {'✅' if orphan == 0 else '❌'} 学生-级号关联: {orphan} 条异常")
    if orphan > 0:
        errors.append(f"{orphan}个学生级号关联异常")

    # 3. 班级履历检查
    print("\n[3] 班级履历检查")

    no_class = db.query("""
        SELECT COUNT(*) as cnt FROM student s
        LEFT JOIN student_class_history sch ON s.student_id = sch.student_id
            AND sch.end_date IS NULL
        WHERE s.status = '在校' AND sch.id IS NULL
    """).first()['cnt']
    print(f"  {'✅' if no_class == 0 else '❌'} 在校学生班级履历: {no_class} 人缺失")
    if no_class > 0:
        errors.append(f"{no_class}个在校学生缺少班级履历")

    # 4. 事件类型检查
    print("\n[4] 事件类型检查")

    positive = db.query("SELECT COUNT(*) as cnt FROM daily_event_type WHERE event_type=1 AND is_active=1").first()['cnt']
    negative = db.query("SELECT COUNT(*) as cnt FROM daily_event_type WHERE event_type=2 AND is_active=1").first()['cnt']
    print(f"  {'✅' if positive > 0 else '⚠️'} 日常积极事件: {positive} 种")
    print(f"  {'✅' if negative > 0 else '⚠️'} 日常消极事件: {negative} 种")

    if positive == 0:
        warnings.append("未配置日常积极事件类型")
    if negative == 0:
        warnings.append("未配置日常消极事件类型")

    # 5. 结果汇总
    print("\n" + "=" * 60)
    print("验证结果")
    print("=" * 60)

    if not errors and not warnings:
        print("✅ 所有检查通过，系统可以正常运行！")
    else:
        if errors:
            print(f"\n❌ 错误 ({len(errors)} 条):")
            for e in errors:
                print(f"   - {e}")
        if warnings:
            print(f"\n⚠️ 警告 ({len(warnings)} 条):")
            for w in warnings:
                print(f"   - {w}")

    return {'errors': errors, 'warnings': warnings}
```

### 11.3 验证API

```python
@router.get("/validate")
async def validate_system_data(current_user: User = Depends(get_current_user)):
    """
    执行数据验证（管理员、学发部）
    """
    if not _user_has_any_role(current_user, ["admin", "xuefa"]):
        raise HTTPException(403, "无权限")

    result = validate_data(db)
    return result
```

### 11.4 系统初始化流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    系统初始化流程                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  开始                                                       │
│    ↓                                                        │
│  ┌─────────────────┐                                       │
│  │ 1. 学年配置     │ ← 无依赖                              │
│  └────────┬────────┘                                       │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │ 2. 级号配置     │ ← 无依赖                              │
│  └────────┬────────┘                                       │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │ 3. 学期配置     │ ← 依赖：学年                          │
│  └────────┬────────┘                                       │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │ 4. 班级信息导入 │ ← 依赖：级号                          │
│  └────────┬────────┘                                       │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │ 5. 学生信息导入 │ ← 依赖：级号、班级                    │
│  └────────┬────────┘                                       │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │ 6. 事件类型配置 │ ← 无依赖，随时可配置                  │
│  └────────┬────────┘                                       │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │ 7. 数据验证     │ ← 检查所有数据完整性                  │
│  └────────┬────────┘                                       │
│           ↓                                                 │
│  ✅ 完成初始化，系统可用                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 十二、方案总结

| 模块 | 核心设计 |
|------|---------|
| **评分体系** | 基础分60 + 四类事件加减分 + 等级评定（可超100分） |
| **任务管理** | 以学年为单位，未完成任务可结转，每次×60%，最多2次 |
| **学期流转** | 荣誉/处分不结转，记录保留在原学期 |
| **班级管理** | 履历表记录历史，快照保证统计准确，留级级号变大 |
| **处分处理** | 可撤销不返分，累进加重机制 |
| **集体事件** | 班级集体荣誉/活动，每人统一加分 |
| **权限控制** | 基于角色的数据可见性配置 |
| **预警机制** | 内部预警，不通知家长 |
| **数据展示** | 个人报告、班级统计、年级对比、趋势分析、导出打印 |
| **数据完整性** | 唯一约束防重复、分数范围校验、状态枚举限制 |
| **批量操作** | 支持批量导入、模板下载 |
| **系统初始化** | 导入依赖检查、分步引导、自动创建班级履历 |
| **事件配置** | 界面化管理、分值约束、累进规则配置 |
| **数据验证** | 多维度检查、错误警告分级、验证报告 |
| **生日提醒** | 自动检测当日生日学生，发送祝福通知 |
| **学生画像** | AI分析生成个性化画像，动态更新标签 |
| **AI诊疗** | 针对学生问题提供智能咨询和解决方案 |

---

## 十三、生日提醒功能

### 13.1 功能概述

生日提醒功能自动检测当日生日的学生，向班主任、学发部发送提醒，并可自动向学生发送生日祝福（需学生绑定微信）。

### 13.2 数据库设计

```sql
-- 生日提醒配置表
CREATE TABLE birthday_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    config_type VARCHAR(20) NOT NULL COMMENT 'reminder/blessing/template',
    is_enabled TINYINT DEFAULT 1 COMMENT '是否启用',
    notify_roles JSON COMMENT '通知角色列表',
    blessing_template TEXT COMMENT '祝福语模板',
    reminder_time VARCHAR(10) DEFAULT '08:00' COMMENT '每日提醒时间',
    advance_days INT DEFAULT 0 COMMENT '提前提醒天数',
    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW()
);

-- 生日祝福记录表
CREATE TABLE birthday_log (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    birthday_date DATE NOT NULL,
    blessing_type VARCHAR(20) COMMENT 'wechat/system/email',
    blessing_content TEXT,
    sent_at DATETIME,
    sent_to VARCHAR(50) COMMENT '发送对象',
    status TINYINT DEFAULT 1 COMMENT '发送状态',
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id),
    INDEX idx_birthday_date (birthday_date)
);

-- 初始化配置
INSERT INTO birthday_config (config_type, is_enabled, notify_roles, blessing_template) VALUES
('reminder', 1, '["cleader", "xuefa"]', '今日是 {student_name} 的生日，请关注'),
('blessing', 1, '["student"]', '🎂 生日快乐！\n亲爱的 {student_name}，今天是你的生日，祝你生日快乐，学业进步，前程似锦！\n\n——来自德育评价系统');
```

### 13.3 功能流程

```
┌─────────────────────────────────────────────────────────────┐
│                    生日提醒流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  每日定时任务（08:00执行）                                   │
│       ↓                                                     │
│  查询今日生日学生列表                                        │
│       ↓                                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 遍历每位生日学生                                      │   │
│  │   ├── 通知班主任（微信/系统消息）                    │   │
│  │   ├── 通知学发部（可选）                              │   │
│  │   └── 发送学生祝福（需学生绑定微信）                  │   │
│  └─────────────────────────────────────────────────────┘   │
│       ↓                                                     │
│  记录发送日志                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 13.4 API设计

```python
@router.get("/birthday/today")
async def get_today_birthdays(current_user: User = Depends(get_current_user)):
    """获取今日生日学生列表（班主任、学发部、管理员）"""
    if not _user_has_any_role(current_user, ["admin", "xuefa", "cleader", "jiaowu"]):
        raise HTTPException(403, "无权限")

    today = datetime.now().strftime('%m-%d')
    # 查询今日生日的学生
    students = db.query("""
        SELECT s.student_id, s.name, s.birthday,
               c.class_id, c.class_name, g.grade_name,
               sch.class_id as current_class_id
        FROM student s
        JOIN student_class_history sch ON s.student_id = sch.student_id
            AND sch.end_date IS NULL
        JOIN class c ON sch.class_id = c.class_id
        JOIN grade g ON s.grade_id = g.grade_id
        WHERE DATE_FORMAT(s.birthday, '%m-%d') = ?
        AND s.status = '在校'
    """, today).all()

    return {"date": today, "count": len(students), "students": students}

@router.get("/birthday/upcoming")
async def get_upcoming_birthdays(days: int = 7):
    """获取近期生日学生列表"""
    # ...

@router.post("/birthday/send-blessing/{student_id}")
async def send_birthday_blessing(student_id: str):
    """手动发送生日祝福"""
    # ...

@router.put("/birthday/config")
async def update_birthday_config(config: BirthdayConfigUpdate):
    """更新生日提醒配置（管理员）"""
    # ...
```

### 13.5 前端界面

```
┌─────────────────────────────────────────────────────────────┐
│  今日生日提醒                                    2026-03-28 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🎂 今日有 3 位学生生日                                     │
│                                                             │
│  ┌─────┬────────┬──────┬─────────────┬──────────┐          │
│  │ 学号│ 姓名    │ 年级 │ 班级        │ 操作     │          │
│  ├─────┼────────┼──────┼─────────────┼──────────┤          │
│  │ S001│ 张三    │ 高一 │ 2025级1班   │ [发送祝福]│          │
│  │ S002│ 李四    │ 高二 │ 2024级3班   │ [发送祝福]│          │
│  │ S003│ 王五    │ 高三 │ 2023级2班   │ [发送祝福]│          │
│  └─────┴────────┴──────┴─────────────┴──────────┘          │
│                                                             │
│  [批量发送祝福]  [查看本周生日]  [配置提醒规则]              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 十四、学生个人画像功能

### 14.1 功能概述

学生个人画像功能基于学生的德育数据、学业数据、行为记录等，通过AI分析生成个性化的学生画像，帮助教师全面了解学生特点，为个性化教育提供依据。

### 14.2 画像维度

| 维度 | 数据来源 | 分析内容 |
|------|---------|---------|
| **品德特征** | 德育记录、荣誉处分 | 责任感、诚信度、友善度、自律性 |
| **学习态度** | 任务完成、日常表现 | 积极性、主动性、毅力、专注度 |
| **社交能力** | 集体活动参与 | 合作精神、领导力、沟通能力 |
| **成长趋势** | 各学期得分变化 | 进步/退步趋势、稳定性 |
| **风险预警** | 消极事件频率 | 行为风险、心理风险 |
| **潜力评估** | 综合数据 | 发展潜力、优势领域 |

### 14.3 数据库设计

```sql
-- 学生画像表
CREATE TABLE student_profile (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    profile_version INT DEFAULT 1,

    -- AI生成的画像内容
    profile_summary TEXT COMMENT '画像摘要（200字以内）',
    profile_tags JSON COMMENT '画像标签数组',
    strength_tags JSON COMMENT '优势标签',
    improvement_tags JSON COMMENT '待改进标签',
    risk_level VARCHAR(10) COMMENT '风险等级：low/medium/high',

    -- 各维度评分（0-100）
    moral_score DECIMAL(5,2) COMMENT '品德评分',
    attitude_score DECIMAL(5,2) COMMENT '态度评分',
    social_score DECIMAL(5,2) COMMENT '社交评分',
    growth_score DECIMAL(5,2) COMMENT '成长评分',

    -- AI生成的建议
    suggestions TEXT COMMENT '个性化建议',
    intervention_priority VARCHAR(10) COMMENT '干预优先级：none/low/medium/high',

    -- 元数据
    data_source_summary JSON COMMENT '数据来源统计',
    generated_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW(),

    FOREIGN KEY (student_id) REFERENCES student(student_id),
    UNIQUE KEY uk_student_version (student_id, profile_version)
);

-- 画像历史表（保留历史版本）
CREATE TABLE student_profile_history (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    profile_version INT NOT NULL,
    profile_data JSON COMMENT '完整画像数据快照',
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (student_id) REFERENCES student(student_id)
);

-- 画像配置表
CREATE TABLE profile_config (
    id INT PRIMARY KEY AUTO_INCREMENT,
    config_key VARCHAR(50) NOT NULL,
    config_value JSON,
    description TEXT,
    UNIQUE KEY uk_key (config_key)
);

INSERT INTO profile_config VALUES
(1, 'tag_definitions', '["责任担当", "诚实守信", "乐于助人", "勤奋刻苦", "积极进取", "团结协作", "遵纪守法", "文明礼貌", "关爱他人", "勇于创新"]', '画像标签定义'),
(2, 'update_frequency', '{"type": "semester", "min_records": 5}', '更新频率配置'),
(3, 'risk_thresholds', '{"high": {"negative_count": 10, "score_below": 50}, "medium": {"negative_count": 5, "score_below": 60}}', '风险阈值配置');
```

### 14.4 AI画像生成流程

```
┌─────────────────────────────────────────────────────────────┐
│                    学生画像生成流程                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 数据收集                                                │
│     ├── 德育评价数据（各学期得分、等级）                    │
│     ├── 日常行为记录（积极/消极事件统计）                   │
│     ├── 校级事件记录（荣誉/处分）                           │
│     ├── 任务完成情况                                        │
│     └── 集体活动参与情况                                    │
│       ↓                                                     │
│  2. 数据分析                                                │
│     ├── 统计各维度数据                                      │
│     ├── 计算趋势变化                                        │
│     ├── 识别行为模式                                        │
│     └── 检测风险信号                                        │
│       ↓                                                     │
│  3. AI画像生成                                              │
│     ├── 调用AI模型分析                                      │
│     ├── 生成画像摘要                                        │
│     ├── 提取特征标签                                        │
│     └── 生成个性化建议                                      │
│       ↓                                                     │
│  4. 画像存储与展示                                          │
│     ├── 保存当前版本                                        │
│     ├── 归档历史版本                                        │
│     └── 更新学生表画像字段                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 14.5 API设计

```python
@router.get("/profile/{student_id}")
async def get_student_profile(student_id: str, current_user: User = Depends(...)):
    """获取学生画像（班主任、学发部、管理员、学生本人）"""
    # 权限检查：学生只能查看自己的画像
    # ...

@router.post("/profile/generate/{student_id}")
async def generate_student_profile(student_id: str):
    """手动生成/更新学生画像（学发部、管理员）"""
    # ...

@router.post("/profile/batch-generate")
async def batch_generate_profiles(class_id: int = None, grade_id: int = None):
    """批量生成班级/年级画像（学发部、管理员）"""
    # ...

@router.get("/profile/history/{student_id}")
async def get_profile_history(student_id: str):
    """获取画像历史版本"""
    # ...

@router.get("/profile/compare/{student_id}")
async def compare_profile_versions(student_id: str, v1: int, v2: int):
    """对比两个版本的画像变化"""
    # ...
```

### 14.6 前端界面

```
┌─────────────────────────────────────────────────────────────┐
│  学生画像 - 张三 (S001)                         高一·2025级1班│
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📊 综合评分                                                │
│  ┌────────────────────────────────────────────────────────┐│
│  │ 品德: ████████░░ 80分    态度: █████████░ 90分         ││
│  │ 社交: ███████░░░ 70分    成长: █████████░ 88分         ││
│  └────────────────────────────────────────────────────────┘│
│                                                             │
│  🏷️ 特征标签                                                │
│  ┌────────────────────────────────────────────────────────┐│
│  │ 优势：[责任担当] [诚实守信] [勤奋刻苦] [积极进取]      ││
│  │ 待改进：[时间管理] [情绪控制]                          ││
│  └────────────────────────────────────────────────────────┘│
│                                                             │
│  📝 画像摘要                                                │
│  张三同学品德表现良好，责任心强，多次主动承担班级事务。    │
│  学习态度积极，任务完成率高。但在时间管理方面有待提升，    │
│  建议制定学习计划，提高效率。社交能力中等，可多参与        │
│  集体活动提升沟通能力。                                    │
│                                                             │
│  ⚠️ 风险等级：低                                            │
│                                                             │
│  💡 个性化建议                                              │
│  1. 建议参与班级学习小组，提升协作能力                      │
│  2. 制定每日学习计划，改善时间管理                          │
│  3. 保持积极态度，争取更多校级荣誉                          │
│                                                             │
│  [查看历史版本] [生成新画像] [导出画像报告]                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 十五、AI诊疗功能

### 15.1 功能概述

AI诊疗功能针对学生的问题（学业、行为、心理等），通过AI智能分析，为教师和家长提供专业的咨询建议和解决方案，帮助及时发现和解决学生成长中的问题。

### 15.2 数据库设计

```sql
-- AI诊疗会话表
CREATE TABLE ai_consultation (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id VARCHAR(20) NOT NULL,
    consultation_type VARCHAR(20) NOT NULL COMMENT 'academic/behavior/psychological/comprehensive',
    title VARCHAR(100) COMMENT '问题标题',
    description TEXT COMMENT '问题描述',

    -- 会话状态
    status VARCHAR(20) DEFAULT 'active' COMMENT 'active/resolved/closed',
    priority VARCHAR(10) DEFAULT 'normal' COMMENT 'urgent/high/normal/low',

    -- 参与人员
    creator VARCHAR(20) COMMENT '创建人',
    assignee VARCHAR(20) COMMENT '负责处理人',
    participants JSON COMMENT '参与讨论人员列表',

    -- AI分析结果
    ai_analysis TEXT COMMENT 'AI分析报告',
    ai_suggestions JSON COMMENT 'AI建议列表',
    ai_risk_assessment TEXT COMMENT '风险评估',

    -- 处理结果
    solution TEXT COMMENT '解决方案',
    outcome VARCHAR(20) COMMENT '解决结果：resolved/improved/ongoing/unresolved',
    follow_up_date DATE COMMENT '跟进日期',

    created_at DATETIME DEFAULT NOW(),
    updated_at DATETIME DEFAULT NOW() ON UPDATE NOW(),
    closed_at DATETIME,

    FOREIGN KEY (student_id) REFERENCES student(student_id),
    INDEX idx_student_status (student_id, status)
);

-- AI诊疗对话记录表
CREATE TABLE ai_consultation_message (
    id INT PRIMARY KEY AUTO_INCREMENT,
    consultation_id INT NOT NULL,
    sender_type VARCHAR(20) NOT NULL COMMENT 'user/ai',
    sender_id VARCHAR(20) COMMENT '发送者ID',
    message_type VARCHAR(20) DEFAULT 'text' COMMENT 'text/analysis/suggestion/report',
    content TEXT NOT NULL,
    metadata JSON COMMENT '消息元数据',
    created_at DATETIME DEFAULT NOW(),
    FOREIGN KEY (consultation_id) REFERENCES ai_consultation(id)
);

-- AI诊疗模板表
CREATE TABLE ai_consultation_template (
    id INT PRIMARY KEY AUTO_INCREMENT,
    template_type VARCHAR(20) NOT NULL COMMENT 'academic/behavior/psychological',
    template_name VARCHAR(50) NOT NULL,
    prompt_template TEXT NOT NULL COMMENT 'AI提示词模板',
    suggested_questions JSON COMMENT '建议提问列表',
    response_format JSON COMMENT '回复格式配置',
    is_active TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT NOW()
);

INSERT INTO ai_consultation_template VALUES
(1, 'academic', '学业问题诊断', '学生{student_name}近期出现学业问题：{description}。请分析可能的原因并提供解决方案。', '["学习成绩下降", "作业完成困难", "课堂注意力不集中"]', '{}', 1, NOW()),
(2, 'behavior', '行为问题诊断', '学生{student_name}出现行为问题：{description}。请分析原因并提供干预建议。', '["迟到早退", "违纪行为", "人际关系冲突"]', '{}', 1, NOW()),
(3, 'psychological', '心理问题诊断', '学生{student_name}可能出现心理困扰：{description}。请提供初步评估和建议。', '["情绪波动", "社交退缩", "压力过大"]', '{}', 1, NOW());
```

### 15.3 AI诊疗流程

```
┌─────────────────────────────────────────────────────────────┐
│                    AI诊疗流程                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 问题提交                                                │
│     ├── 选择问题类型（学业/行为/心理/综合）                │
│     ├── 描述问题描述                                        │
│     └── 上传相关证据（可选）                                │
│       ↓                                                     │
│  2. AI初步分析                                              │
│     ├── 自动关联学生画像                                    │
│     ├── 分析历史数据                                        │
│     ├── 生成问题诊断                                        │
│     └── 提供初步建议                                        │
│       ↓                                                     │
│  3. 深度咨询                                                │
│     ├── 教师与AI对话                                        │
│     ├── AI提供详细分析                                      │
│     ├── 生成解决方案                                        │
│     └── 评估风险等级                                        │
│       ↓                                                     │
│  4. 方案实施                                                │
│     ├── 制定行动计划                                        │
│     ├── 分配责任人                                          │
│     ├── 设置跟进日期                                        │
│       ↓                                                     │
│  5. 效果评估                                                │
│     ├── 记录实施效果                                        │
│     ├── 更新学生画像                                        │
│     └── 生成处理报告                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 15.4 API设计

```python
@router.post("/consultation/create")
async def create_consultation(consultation: ConsultationCreate):
    """创建诊疗会话（班主任、学发部、教发部、管理员）"""
    # ...

@router.get("/consultation/{consultation_id}")
async def get_consultation(consultation_id: int):
    """获取诊疗会话详情"""
    # ...

@router.post("/consultation/{consultation_id}/message")
async def send_consultation_message(consultation_id: int, message: MessageInput):
    """发送诊疗消息（用户与AI对话）"""
    # ...

@router.post("/consultation/{consultation_id}/ai-analyze")
async def trigger_ai_analysis(consultation_id: int):
    """触发AI深度分析"""
    # ...

@router.put("/consultation/{consultation_id}/solution")
async def update_solution(consultation_id: int, solution: SolutionUpdate):
    """更新解决方案"""
    # ...

@router.post("/consultation/{consultation_id}/close")
async def close_consultation(consultation_id: int, outcome: str):
    """关闭诊疗会话"""
    # ...

@router.get("/consultation/student/{student_id}")
async def get_student_consultations(student_id: str):
    """获取学生的所有诊疗记录"""
    # ...
```

### 15.5 AI诊疗界面

```
┌─────────────────────────────────────────────────────────────┐
│  AI诊疗 - 张三 (S001)                           [行为问题]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  问题描述                                                   │
│  ┌────────────────────────────────────────────────────────┐│
│  │ 近一周内迟到3次，课堂表现消极，与同学发生冲突           ││
│  └────────────────────────────────────────────────────────┘│
│                                                             │
│  🤖 AI分析报告                                              │
│  ┌────────────────────────────────────────────────────────┐│
│  │ 【问题诊断】                                            ││
│  │ 根据学生画像和历史数据分析，张三近期行为异常可能与      ││
│  │ 以下因素相关：                                          ││
│  │ 1. 学习压力增大（近期考试频繁）                         ││
│  │ 2. 人际关系问题（与同桌关系紧张）                       ││
│  │ 3. 情绪调节能力不足                                     ││
│  │                                                         ││
│  │ 【风险评估】                                            ││
│  │ 风险等级：中等                                          ││
│  │ 建议关注：情绪管理、人际关系改善                         ││
│  │                                                         ││
│  │ 【建议措施】                                            ││
│  │ 1. 与学生进行单独谈话，了解真实原因                      ││
│  │ 2. 调换座位，改善同桌关系                                ││
│  │ 3. 安排心理辅导老师介入                                  ││
│  │ 4. 制定行为改进计划，设置奖励机制                        ││
│  └────────────────────────────────────────────────────────┘│
│                                                             │
│  💬 对话记录                                                │
│  ┌────────────────────────────────────────────────────────┐│
│  │ [班主任] 10:30: 孩子最近总是迟到，怎么办？              ││
│  │ [AI] 10:31: 建议先了解迟到原因...                       ││
│  │ [班主任] 10:35: 他说早上起不来                          ││
│  │ [AI] 10:36: 可能与作息有关，建议...                     ││
│  └────────────────────────────────────────────────────────┘│
│                                                             │
│  [输入消息...] [发送] [请求AI分析] [导出报告]               │
│                                                             │
│  处理状态：进行中    负责人：班主任王老师    跟进日期：+7天  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 15.6 权限配置

```yaml
# AI诊疗功能权限配置
/consultation:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/consultation/{id}:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0
  # 班主任只能查看本班学生的诊疗

/consultation/create:
  allowed_roles: [cleader, xuefa, jiaowu, admin]
  min_level: 0

/consultation/{id}/ai-analyze:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5

/consultation/templates:
  allowed_roles: [xuefa, jiaowu, admin]
  min_level: 5
```

---

## 十六、角色权限扩展

### 16.1 角色定义

| 角色ID | 角色名称 | 说明 | 主要职责 |
|--------|---------|------|---------|
| admin | 管理员 | 系统管理员 | 系统配置、数据管理、权限分配 |
| jiaowu | 教发部 | 教师发展部 | 教学质量监控、教师管理、学业诊断 |
| xuefa | 学发部 | 学生发展部 | 德育评价管理、学生管理、行为诊断 |
| cleader | 班主任 | 班级管理者 | 本班学生管理、日常记录、诊疗发起 |
| teacher | 教师 | 普通教师 | 查看班级数据、日常记录录入 |
| student | 学生 | 学生 | 查看个人数据、接收消息 |
| parent | 家长 | 学生家长 | 查看子女数据、接收通知 |

### 16.2 功能权限矩阵

| 功能模块 | admin | jiaowu | xuefa | cleader | teacher | student | parent |
|---------|-------|--------|-------|---------|---------|---------|--------|
| 系统配置 | ✓ | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ |
| 学年/级号管理 | ✓ | ✓ | ✓ | ✗ | ✗ | ✗ | ✗ |
| 班级管理 | ✓ | ✓ | ✓ | 本班 | ✗ | ✗ | ✗ |
| 学生信息管理 | ✓ | ✓ | ✓ | 本班 | ✗ | 本人 | 子女 |
| 德育记录录入 | ✓ | ✓ | ✓ | ✓ | ✓ | ✗ | ✗ |
| 德育记录修改 | ✓ | ✓ | ✓ | 本班 | ✗ | ✗ | ✗ |
| 评价查询 | ✓ | ✓ | ✓ | 本班 | 本班 | 本人 | 子女 |
| 数据导出 | ✓ | ✓ | ✓ | 本班 | ✗ | ✗ | ✗ |
| 生日提醒 | ✓ | ✓ | ✓ | 本班 | ✗ | ✗ | ✗ |
| 学生画像 | ✓ | ✓ | ✓ | 本班 | 本班 | 本人 | 子女 |
| AI诊疗创建 | ✓ | ✓ | ✓ | 本班 | ✗ | ✗ | ✗ |
| AI诊疗查看 | ✓ | ✓ | ✓ | 本班 | ✗ | ✗ | ✗ |
| AI诊疗分析 | ✓ | ✓ | ✓ | ✗ | ✗ | ✗ | ✗ |
| 事件类型配置 | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ | ✗ |

### 16.3 数据可见性配置

```sql
-- 更新数据可见性配置
INSERT INTO data_visibility_config (data_type, visible_roles, description) VALUES
('birthday_list', '["admin", "xuefa", "jiaowu", "cleader"]', '生日提醒列表'),
('student_profile', '["admin", "xuefa", "jiaowu", "cleader", "student", "parent"]', '学生画像'),
('ai_consultation', '["admin", "xuefa", "jiaowu", "cleader"]', 'AI诊疗记录'),
('consultation_detail', '["admin", "xuefa", "jiaowu", "cleader_creator"]', '诊疗详情（班主任只能看自己创建的）');
```

---

## 十七、扩展功能API汇总

### 17.1 学生扩展信息API

```python
@router.put("/students/{student_id}/extended-info")
async def update_student_extended_info(student_id: str, info: StudentExtendedInfo):
    """更新学生扩展信息（入学成绩、高考成绩、初中学校、大学院校）"""
    # 权限：学发部、教发部、管理员

@router.get("/students/{student_id}/full-info")
async def get_student_full_info(student_id: str):
    """获取学生完整信息（含扩展信息）"""

@router.get("/students/graduates")
async def get_graduate_students(year: int = None):
    """获取毕业生列表（含高考成绩、大学去向）"""

@router.get("/statistics/university")
async def get_university_statistics(grade_id: int = None):
    """获取大学录取统计"""
```

### 17.2 学生信息导入模板（扩展版）

```
导入模板（Excel）- 学生完整信息：

| 学号 | 姓名 | 性别 | 级号 | 班号 | 入学日期 | 出生日期 | 入学成绩 | 初中学校 |
|------|------|------|------|------|---------|---------|---------|---------|
| S001 | 张三 | 男   | 2025级 | 1   | 2025-09-01 | 2008-03-15 | 520.5 | XX中学 |
| S002 | 李四 | 女   | 2025级 | 1   | 2025-09-01 | 2008-05-20 | 515.0 | YY中学 |

毕业生补充信息模板：

| 学号 | 高考成绩 | 高考年份 | 高考排名 | 大学名称 | 院校类型 | 录取专业 |
|------|---------|---------|---------|---------|---------|---------|
| S001 | 620.5  | 2028    | 1500    | XX大学   | 211     | 计算机  |
```

---

## 十八、定时任务配置

### 18.1 定时任务列表

| 任务名称 | 执行时间 | 功能说明 |
|---------|---------|---------|
| birthday_reminder | 每日 08:00 | 检测今日生日学生，发送提醒 |
| birthday_blessing | 每日 08:30 | 向学生发送生日祝福 |
| profile_update_check | 每周一 09:00 | 检查需要更新画像的学生 |
| semester_evaluation | 学期末 | 计算学期德育评价 |
| task_carryover | 学年末 | 处理未完成任务结转 |
| warning_check | 每日 10:00 | 检查预警阈值 |

### 18.2 定时任务实现

```python
# 定时任务配置
from apscheduler.schedulers.background import BackgroundScheduler

scheduler = BackgroundScheduler()

# 生日提醒任务
@scheduler.scheduled_job('cron', hour=8, minute=0)
async def birthday_reminder_task():
    """每日生日提醒"""
    today = datetime.now().strftime('%m-%d')
    birthday_students = get_birthday_students(today)

    for student in birthday_students:
        # 通知班主任
        notify_class_leader(student)
        # 通知学发部（可选）
        notify_xuefa(student)
        # 记录日志
        log_birthday_reminder(student)

# 生日祝福任务
@scheduler.scheduled_job('cron', hour=8, minute=30)
async def birthday_blessing_task():
    """发送生日祝福"""
    config = get_birthday_config()
    if not config['blessing_enabled']:
        return

    today = datetime.now().strftime('%m-%d')
    students = get_birthday_students(today)

    for student in students:
        if student.wxid:  # 学生已绑定微信
            blessing = config['blessing_template'].format(
                student_name=student.name
            )
            send_wechat_message(student.wxid, blessing)
            log_birthday_blessing(student, blessing)

# 画像更新检查任务
@scheduler.scheduled_job('cron', day_of_week='mon', hour=9)
async def profile_update_task():
    """检查需要更新画像的学生"""
    config = get_profile_config()
    # 查找新记录超过阈值的学生
    students_need_update = find_students_need_profile_update()

    for student_id in students_need_update:
        generate_student_profile(student_id)
```

---

*文档版本：v1.2*
*最后更新：2025-03-26*